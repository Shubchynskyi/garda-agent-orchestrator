<!-- garda-agent-orchestrator:managed-start -->
# TASK.md

Single-file task queue for local agent orchestration.
This file is intentionally expected to be gitignored.
Do not force-add it to git unless the user explicitly asks to version orchestration control-plane files.

Canonical instructions entrypoint for orchestration: `AGENTS.md`.
Hard stop: first open `AGENTS.md` and follow its routing links. Only then execute any task from `TASK.md`.
Orchestrator mode starts when task execution is requested from this file (`TASK.md`).
If needed, the agent can add new tasks from user requests and then execute them in orchestrator mode.
Task timeline log (per task): `garda-agent-orchestrator/runtime/task-events/<task-id>.jsonl`.

## Status Legend
- `🟦 TODO`
- `🟨 IN_PROGRESS`
- `🟧 IN_REVIEW`
- `🟩 DONE`
- `🟥 BLOCKED`
- `🟫 SPLIT_REQUIRED`
- `🟪 DECOMPOSED`

Status cell format rule: keep canonical token, optionally prefixed by marker (for example `🟦 TODO`).

## Active Queue
| ID | Status | Priority | Area | Title | Owner | Updated | Profile | Notes |
|---|---|---|---|---|---|---|---|---|
| T-565 | 🟩 DONE | P1 | tests/ci-generated-build-contracts | Fix CI-only generated-test contract regressions for human-commit docs path, setup handoff prompt, and workflow help aliases                                                                                              | gpt-5.3-codex | 2026-05-18 | balanced | CI failure after T-240: `.node-build/tests/node/cli/commands/gates.test.js` cannot open duplicated `garda-agent-orchestrator/garda-agent-orchestrator/live/docs/agent-rules/40-commands.md`; `setup.test.js` no longer sees `Project memory init/refresh prompt: ${PROJECT_MEMORY_INIT_REFRESH_PROMPT}` in setup handoff; `workflow.test.js` no longer sees `--scope-budget on\|off\|--scope-budget-enabled true\|false` in workflow help. Guardrails: targeted CI/test-contract fix only; do not weaken operator confirmation, project-memory prompt, workflow help, full-suite, or review policy behavior. |
| T-566 | 🟩 DONE | P1 | ux/update-apply-version-reporting-and-color | Fix stale update apply version reporting and highlight update status/new-version messages                                                                                                                                | gpt-5.4 | 2026-05-18 | balanced | Operator report from Chronobot update on 2026-05-18: `CurrentVersion: 1.0.0`, `LatestVersion: 1.1.0`, `UpdateApplied: True`, and `CheckUpdateResult: UPDATED`, but plain output still printed `UpdatedVersion: 1.0.0` even though the deployed bundle later reported `1.1.0`. Fix update/apply reporting so `UpdatedVersion` is read after deferred `VERSION` and `live/version.json` sync, and add regressions for `update git` / `check-update --apply` or the shared path that currently reports stale lifecycle output. Also improve user-facing coloring: make successful applied-update status, available-new-version notices, and old->new version deltas visually distinct in colored output while preserving stable plain/no-color and JSON fields. Guardrails: do not change update trust policy, rollback behavior, version comparison semantics, workflow-config merge preservation, or machine-readable key names; keep no-color output deterministic for tests and logs; do not hide warnings or failures behind success coloring; add tests proving stale `UpdatedVersion` cannot survive a successful apply and that color is applied only in colored human output. |
| T-567 | 🟩 DONE | P1 | ui/local-server-idle-shutdown | Add server-owned inactivity detection and auto-shutdown UX to `garda ui`                                                                                                                                                 | gpt-5.4 | 2026-05-18 | strict | Operator request: `garda ui` should not stay alive indefinitely, but should not require a persistent lightweight standby server after expiry. Add server-owned inactivity tracking with browser-visible countdown: the browser listens for activity events and sends throttled heartbeat/activity pings; the server stores `last_activity_at`, configurable idle threshold, warning countdown length, and shutdown deadline; after `idle_minutes` without activity the page enters warning state and shows a slider/countdown that decreases toward shutdown; any real user activity before the deadline resets/extends the server-side timer; when the deadline expires the server closes the HTTP server/process. Plan: 1) add session state to the local UI server with monotonic server-side expiry checks, default idle threshold and warning duration; 2) add read-only endpoints such as `GET /api/session`, `POST /api/session/activity`, and guarded `POST /api/session/shutdown` if needed, with no arbitrary command execution; 3) add frontend activity listeners, throttled heartbeat, right-side `Server Status`, warning slider/countdown, and exact stopped message telling the user to rerun `garda ui --target-root "."` because in-page Launch cannot work after full shutdown; 4) keep shutdown source-of-truth on the server so closed tabs, sleeping browsers, or throttled background timers still stop the process; 5) add CLI/help/docs for timeout defaults and any flags such as `--idle-minutes` or `--no-idle-shutdown` only if policy allows; 6) add tests for activity reset, idle warning, countdown expiry shutdown, closed-browser/no-heartbeat shutdown, manual stop/shutdown, localhost-only behavior, and no lifecycle/workflow-config mutation. Guardrails: do not leave a hidden daemon/supervisor, do not implement a fake in-page Launch after full server shutdown, do not bind remotely, do not add dependencies, do not let browser input run shell commands, preserve `Ctrl+C` stop behavior, and keep default UI action/config mutation protections intact. |
| T-568 | 🟩 DONE | P2 | ui/local-dashboard-i18n-language-packs | Add extensible `garda ui` internationalization with English and Russian first                                                                                                                                            | gpt-5.4 | 2026-05-18 | balanced | Operator correction: ship only two UI languages now, `en` and `ru`, and add the architecture plus language switcher panel so additional packs such as `de` and `uk` can be added later when the UI copy is stable. Add a small i18n layer for `garda ui` and related static UI-rendered strings where practical: centralize translatable UI copy in a typed dictionary, use English as the fallback/base language, add Russian translations, and make the language selectable from a visible UI panel plus optionally from a CLI flag such as `garda ui --language <en|ru>` or from existing assistant-language/init answers when unambiguous. The language panel should be data-driven so future languages appear by adding a language pack and metadata, not by rewriting dashboard markup. Translate UI chrome only: tabs, headings, buttons, status labels, Server Status / idle shutdown text, workflow-config field descriptions, empty states, validation messages, and short help text. Do not translate CLI commands, task IDs, config keys, enum values, file paths, raw gate/review/artifact output, or machine-readable JSON. Guardrails: no heavy i18n dependency unless explicitly justified; keep missing keys fail-fast in tests and fallback-safe at runtime; preserve no-color/plain CLI contracts; keep language choice local to UI unless a later task defines global localization; add tests for fallback behavior, all required keys present in English/Russian, language selector persistence, extension metadata for future packs, CLI flag parsing if added, and untranslated command/config/raw-output surfaces staying exact. |
| T-569 | 🟩 DONE | P1 | workflow/review-context-default-output-path | Make `build-review-context` create and report a canonical artifact when `--output-path` is omitted                                                                                                                       | gpt-5.4 | 2026-05-18 | strict | Operator correction after delegated-review friction: an agent should not need to know or invent the review-context output path. Teach `gate build-review-context` to use a canonical default path such as `garda-agent-orchestrator/runtime/reviews/<task-id>-<review-type>-review-context.md` or the existing canonical extension/shape when `--output-path` is omitted, then print machine-readable and human-readable `ReviewContextPath` and `ReviewContextSha256`. Keep explicit `--output-path` supported for tests/debug/custom flows. `next-step` should prefer commands that do not require agents to hand-type context paths unless a non-default path is necessary. Guardrails: do not weaken review context binding to current task/preflight/compile cycle; stale context artifacts must be refreshed or rejected clearly; do not silently write outside canonical runtime review paths; keep JSON/no-color output stable; add regressions for omitted output path, explicit output path, stale existing context, path/hash output, and no successful no-op where the context was not created. |
| T-570 | 🟩 DONE | P1 | workflow/reviewer-raw-output-capture | Add a gate-owned way to persist exact reviewer text returned by sub-agents without requiring reviewer file writes                                                                                                        | gpt-5.4 | 2026-05-18 | strict | Operator correction: many delegated sub-agents cannot write files, so the workflow must not require the reviewer itself to create `review-output.md`. Add or refine a gate command/input path so the main agent can pass the exact raw reviewer response text to Garda, and Garda writes the canonical raw-output artifact under runtime review storage before materializing `record-review-result`. The main agent may transport the reviewer response, but must not rewrite, complete, summarize, or repair the verdict on the reviewer's behalf; invalid format should fail clearly and route to a real reviewer rerun or an operator-visible blocker. Relates to `T-198` but is not a duplicate: `T-198` focuses on lossless fidelity for path-based PASS materialization, while this task focuses on provider-neutral capture when the sub-agent returns text instead of writing a file. Guardrails: preserve raw reviewer evidence, independent reviewer provenance, exact verdict-token checks, output-template validation, and deferred-finding enforcement; do not allow same-agent fabricated output; do not require root scratch files; add regressions for text/stdin or equivalent raw-response capture, invalid reviewer format failure, no main-agent normalization, canonical artifact path, and compatibility with existing `--review-output-path`. |
| T-571 | 🟩 DONE | P1 | workflow/gate-owned-review-trust-ux | Hide reviewer trust-chain and timestamp mechanics behind gates with clear spoof/fresh-review diagnostics                                                                                                                 | gpt-5.4 | 2026-05-18 | strict | Operator correction: agents should not reason about `launched_at_utc`, timestamp ordering, or the internal `routing -> prepare -> complete -> invocation -> result` trust chain. Agents must not supply launch timestamps manually; timing fields such as prepared/launch/recorded timestamps must be written by Garda gates/runtime using system time at the moment the relevant gate action happens. Refactor the review flow UX so `next-step` and gate commands expose only simple actions: prepare reviewer context/launch, launch the delegated reviewer with the printed prompt/context, record the exact reviewer output, and run required review check. Timing/order validation remains internal and gate-owned: after review output is recorded, gates verify that context, launch evidence, raw output, receipt/result, preflight, and compile cycle are coherent. If timing/order evidence clearly indicates stale or fabricated review, the gate should tell the agent to run a real fresh delegated review or report the integrity blocker to the user, without requiring the agent to understand why a timestamp comparison failed. Guardrails: do not weaken reviewer independence, launch attestation, receipt validation, review reuse policy, stale-cycle rejection, or fake artifact protection; do not ask agents to manually capture or stabilize timestamps; do not accept agent-provided timestamps for review launch evidence except legacy compatibility paths that are clearly marked and migrated away; do not expose low-level trust-chain jargon in normal happy-path output; keep detailed diagnostics available for debug/audit; add regressions for system-time timestamp ownership, happy-path simplified commands, spoofed/stale launch evidence, missing real delegated review, stale previous reviewer output, and clear remediation wording. |
| T-572 | 🟩 DONE | P1 | workflow/full-suite-after-real-diff | Do not run mandatory full-suite validation on baseline-only pre-implementation cycles                                                                                                                                    | gpt-5.4 | 2026-05-18 | strict | Operator correction from T-569: `full-suite-validation` must still run before required reviews when there are real implementation changes, but `next-step` must not force `npm test` on a baseline-only preflight before the task has changed anything. Fix lifecycle routing so full-suite placement `after_compile_before_reviews` means after a current implementation compile for the active changed scope, not after an empty baseline compile. If the workspace/preflight is `BASELINE_ONLY`, route to implementation/refresh-preflight/no-op evidence as appropriate instead of spending full-suite time; once real changes exist, full-suite remains mandatory before reviews and closeout reuse rules stay strict. Guardrails: do not weaken full-suite requirements for code/test/config/docs changes that policy requires; do not let agents skip full-suite after implementation; preserve compile-before-suite ordering, review-before-completion ordering, timeout forecast output, and stale-artifact rejection; add regressions for baseline-only no full-suite route, real diff still requires full-suite before reviews, stale baseline suite cannot satisfy post-change validation, and no docs-only/protected-control-plane false positives unless policy explicitly requires them. |
| T-573 | 🟩 DONE | P1 | workflow/review-routing-phase-lock | Block late reviewer rerouting after a review result has been recorded                                                                                                                                                    | gpt-5.4 | 2026-05-18 | balanced | Operator correction from T-569: `record-review-routing` must not allow a new `REVIEWER_DELEGATION_ROUTED` event for the same task/review type after a current-cycle `REVIEW_RECORDED` exists, because that can make `required-reviews-check` pass while `completion-gate` later sees a different latest reviewer identity. Add a phase lock so late rerouting is rejected with a clear recovery message unless the workflow is explicitly in a fresh review/restart cycle that invalidates downstream evidence first. Guardrails: do not weaken valid reruns before a review result is recorded; preserve review reuse and restart semantics; do not silently select an older matching routing event when a later conflicting event exists; add regressions for same-review late reroute rejection, allowed reroute before result, different review types remaining independent, and clear remediation wording that avoids unnecessary full task reset. |
| T-574 | 🟩 DONE | P1 | workflow/review-gate-completion-parity | Make `required-reviews-check` and `completion-gate` enforce the same reviewer provenance rules                                                                                                                           | gpt-5.4 | 2026-05-18 | balanced | Operator correction from T-569: `required-reviews-check` passed even though `completion-gate` later rejected inconsistent reviewer identity between current routing telemetry and the recorded review context/receipt. Align these gates so the review gate catches the same provenance, identity, execution-mode, routing-event, invocation, and context-hash inconsistencies before it emits `REVIEW_GATE_PASSED`. Prefer shared validation helpers or a single evidence verdict path over duplicated partial checks. Guardrails: do not weaken completion validation; do not create false blockers for valid current-cycle reused reviews; keep diagnostics concise for the normal path and detailed enough for audit/debug; add regressions where conflicting late routing is rejected at review gate, completion accepts evidence that review gate accepted, and both gates report the same actionable remediation. |
| T-575 | 🟩 DONE | P1 | workflow/pass-review-note-classification | Stop non-actionable PASS review validation-boundary notes from becoming strict F-task blockers                                                                                                                           | gpt-5.4 | 2026-05-18 | balanced | Operator correction from T-569: `record-review-result` told the agent to move a non-blocking validation-boundary note from `Residual Risks` to `Deferred Findings`, then strict `completion-gate` treated that deferred note as a mandatory F task. Fix the contract so non-actionable PASS notes such as "review artifact did not include inline diff" are either accepted as `Residual Risks: none` / `Deferred Findings: none` with the note preserved in raw output, or rejected with wording that explicitly says a real deferred finding will require a follow-up task. Guardrails: do not hide real findings, real deferred work, or accepted technical debt; preserve strict follow-up creation for actionable deferred findings; keep raw reviewer output auditable; add regressions for validation-boundary notes, command/log notes, true deferred actionable findings, and gate messaging that does not guide agents into creating F-task noise. |
| T-576 | 🟩 DONE | P1 | ux/workflow-settings-human-output | Make workflow settings commands readable, colorized, and semantically correct for humans                                                                                                                                 | gpt-5.4 | 2026-05-18 | balanced | Operator report: `garda workflow set --task-reset off` prints `Status: NO_CHANGE` but also `ChangedFields: task_reset.enabled`, which is misleading because the field was requested, not changed. Preliminary grep found no in-repo parser of the plain `ChangedFields:` line; machine consumers use `--json` `changed_fields` and audit JSONL `changed_fields`, especially task-reset audit validation. Improve human/plain and colored output for workflow settings commands (`workflow show`, `workflow set`, `workflow validate`, and allow-listed UI/settings action surfaces if they display the same data): group settings by area, clearly separate requested fields from actually changed fields/no-op, highlight CHANGED/NO_CHANGE/errors, and keep concise next-action hints. Guardrails: preserve JSON output fields, audit JSONL shape, config semantics, operator-confirmation requirements, and no-color deterministic output where tests/logs depend on it; do not rename machine-readable `changed_fields` without a migration task; add regression coverage proving no misleading `ChangedFields` appears for `NO_CHANGE` human output and colored output is disabled under no-color/JSON modes. |
| T-577 | 🟦 TODO | P1 | workflow/workflow-set-protected-manifest-refresh | Treat audited operator settings commands as trusted baseline updates while blocking direct agent JSON edits                                                                                                              | gpt-5.4 | 2026-05-18 | balanced | Operator report from `D:\Projects\Chronobot`: after changing repo-local workflow settings with `workflow set`, agents cannot enter `T-043`; `enter-task-mode` fails with `Workspace already contains workflow config changes before task-mode entry: garda-agent-orchestrator/live/config/workflow-config.json`. Doctor confirms `Protected Control-Plane Manifest: DRIFT` with exactly that file, while `git status --short` is clean. Required contract: settings are operator-owned and must be freely changeable through official audited commands such as `garda workflow set ...`; a successful command-surface settings change must update/reconcile the trusted protected baseline so the next ordinary task can enter task-mode normally. Direct JSON edits by an agent or unaudited pre-task file mutations must still block as laundering risk. Fix the command/gate contract so successful audited `workflow set` refreshes or records the trusted protected manifest for the changed workflow-config file with clear audit evidence, and make `next-step`/doctor explain the distinction between trusted operator command changes and untrusted direct JSON drift. Guardrails: do not weaken protection for manual/un-audited workflow-config edits, do not let agents approve protected control-plane mutations for themselves, preserve `--workflow-config-work` requirements for tasks that intentionally edit workflow config as implementation scope, keep `repair protected-manifest` as an explicit recovery/manual-accept path rather than a mandatory second step after official settings commands, and add deployed-workspace regressions for audited workflow set -> ordinary task-mode entry succeeds while direct JSON edits still block. |
| T-578 | 🟦 TODO | P1 | ux/doctor-readable-failure-summary | Make `garda doctor` failure output concise, colored, and actionable before long evidence dumps                                                                                                                           | gpt-5.4 | 2026-05-18 | balanced | Operator report from Chronobot: `doctor` exits `FAIL`, but the important cause is hard to see in the long output. Current evidence shows the actionable failure is `Protected Control-Plane Manifest` `Status: DRIFT`, `DriftCount: 1`, file `garda-agent-orchestrator/live/config/workflow-config.json`, while most other sections are PASS/healthy. Improve human output so doctor prints a top failure summary first, highlights failing sections/files/remediation, separates PASS evidence from blockers, and suggests the exact next command when a safe repair command exists (for example `garda repair protected-manifest --target-root "." --confirm` after operator verification). Guardrails: preserve `doctor --json` schema for automation, keep detailed evidence available in human output or a verbose/debug mode, do not hide warnings behind green PASS banners, preserve exit codes, and add tests for protected-manifest drift, runtime mismatch, missing paths, and multi-failure ordering. |
| T-579 | 🟦 TODO | P1 | runtime/node-baseline-contract-audit | Audit and align the Node runtime baseline across package engines, doctor, docs, and release validation                                                                                                                   | gpt-5.4 | 2026-05-18 | balanced | Operator question: doctor still prints `Required: >=24.0.0`, but the expected supported version may have moved to Node 20 or Node 22. Current source evidence still says `>=24.0.0` in `package.json`, `package-lock.json`, `src/core/constants.ts`, `docs/node-runtime-contract.md`, and bundled copy; prior memory also recorded the main repo/registry as Node 24 baseline. Decide the intended contract explicitly, then align every source of truth: `NODE_ENGINE_RANGE`, `NODE_BASELINE_LABEL`, package engines/lockfile, docs, doctor runtime mismatch text, release readiness, CI matrix, setup/update generated bundle content, and tests. Guardrails: do not silently lower the baseline without CI/release validation for the new Node line; if Node 20/22 becomes supported, update diagnostics to state support tiers clearly; if Node 24 remains required, update operator-facing docs/messages to explain why and remove stale claims that imply 20/22 support; add regression coverage for the selected range and doctor output. |
| T-580 | 🟦 TODO | P1 | ux/managed-agentignore-active-mode | Add a managed `.agentignore` block that hides bulky Garda artifacts in active mode and extends it in off mode                                                                                                            | gpt-5.4 | 2026-05-18 | balanced | Operator request: manage `.agentignore` like `.gitignore`, with a Garda-owned block for safe active-mode ignores, and when `garda off` is used add the whole `garda-agent-orchestrator/` directory on top of that. Implement an idempotent managed `.agentignore` block during setup/update/reinit/repair or an explicit command path that hides bulky/generated artifacts from agent auto-context while keeping required command/instruction/config surfaces visible. Safe active-mode candidates: `garda-agent-orchestrator/dist/`, `garda-agent-orchestrator/src/`, `garda-agent-orchestrator/template/`, `garda-agent-orchestrator/runtime/update-rollbacks/`, `garda-agent-orchestrator/runtime/tmp/`, `garda-agent-orchestrator/runtime/metrics/`, full-suite logs, scoped diffs, review-context bulk artifacts, aggregate task-event indexes, and timeline summary caches. Do not hide active instructions/config/command surfaces: `AGENTS.md`, `.agents/workflows/start-task.md`, `garda-agent-orchestrator/bin/garda.js`, `garda-agent-orchestrator/live/docs/agent-rules/`, `garda-agent-orchestrator/live/config/`, or `runtime/protected-control-plane-manifest.json`. `garda off` should preserve the active managed block and add a separate/full off-mode ignore for `garda-agent-orchestrator/`; `garda on` should remove only the off-mode full-directory ignore and leave the active safe block. Guardrails: do not make CLI/gates depend on `.agentignore`; do not prevent agents from reading specific runtime evidence paths when `next-step`/doctor explicitly points to them; preserve unrelated user `.agentignore` content; fail closed on malformed managed markers; add tests for fresh creation, idempotent update, on/off transitions, user content preservation, and safe/unsafe path lists. |
| T-581 | 🟦 TODO | P1 | test/flaky-temp-git-fixtures | Stabilize temp git fixture initialization in full-suite tests                                                                                                                                                            | gpt-5.4 | 2026-05-18 | balanced | Operator report from T-573 full-suite: the suite intermittently failed in `gates-protected-drift.test` while initializing a temporary git repository: `git init failed: error: opening .../.git/config: Permission denied` and `fatal: could not set 'core.ignorecase' to 'true'`. The immediate rerun passed, so this looks like recurring flaky Windows temp-dir/git fixture behavior, not a T-573 product failure. Audit and harden temp repository setup for tests that call `initializeGitRepo` / `runGit`: isolate temp roots, avoid races with cleanup/indexers, add safe retry or clearer fixture diagnostics where appropriate, and ensure failures point to the owning fixture/test. Guardrails: do not blanket-rerun or hide real full-suite failures, do not weaken protected-drift assertions, preserve Windows coverage, keep temp cleanup deterministic, and add regressions or fixture-level tests that exercise permission/locked-config recovery without depending on actual OS flakiness. |
| T-582 | 🟩 DONE | P1 | workflow/pass-review-none-normalization | Accept common no-risk PASS review wording without forcing reviewer restatement | gpt-5.4 | 2026-05-19 | balanced | Operator report from T-568: `record-review-result` rejected a no-findings `TEST REVIEW PASSED` because `Residual Risks` said `None significant for the scoped remediation...` instead of exactly `none`. Fix PASS review normalization so empty/no-risk sections accept common no-issue variants such as `none`, `None`, `NONE`, `none.`, and clearly no-active-risk prose like `None significant...`, while preserving strict rejection of real active findings, actionable deferred work, security/risk signals, or non-generic residual risks. Guardrails: do not weaken detection of real findings or true residual technical debt; keep raw reviewer output auditable; do not silently convert actionable deferred findings into none; ensure review receipts still record exact raw output/fidelity; add regressions for case-insensitive none variants, `None significant...` no-risk prose, real residual-risk rejection, and strict profile deferred-finding enforcement. |
| T-583 | 🟩 DONE | P2 | ux/project-memory-impact-updated-file-guidance | Make `project-memory-impact --confirm-updated` print or infer the required updated-memory-file arguments | gpt-5.4 | 2026-05-19 | balanced | Operator report from T-568: after updating the affected project-memory files, `project-memory-impact --confirm-updated` still blocked until the agent discovered that every affected file must be repeated with `--updated-memory-file`. Improve the recovery UX so `next-step` or the blocked gate output prints a ready-to-run command containing all affected `--updated-memory-file` arguments, or safely infers the affected list when `--confirm-updated` is used and the current scope proves those affected files were updated. Guardrails: preserve explicit audit evidence for which memory files were updated; do not allow bare `--confirm-updated` to pass when affected files are missing/stale or unmodified; keep strict/update mode and user-approval semantics; preserve machine-readable artifact fields; add regressions for missing flags guidance, successful ready-command flow, incomplete updated-file list rejection, and no-update-needed tasks. |
| T-584 | 🟩 DONE | P2 | workflow/late-startup-preflight-review-context-invalidation | Prevent late startup/preflight refresh from invalidating fresh review context without explicit scope change | gpt-5.4 | 2026-05-19 | balanced | Observed during T-583: after a code review context was prepared, a later TASK_ENTRY/handshake/shell-smoke/classify-change refresh changed the preflight hash and made the fresh review context stale, forcing duplicate review-lane work. Investigate and fix the navigation/order contract so `next-step` either avoids late startup/preflight refresh after review-context preparation, proves and explains a real scope change before invalidating review bindings, or routes through an explicit coherent-cycle recovery. Guardrails: do not weaken review-context preflight binding, compile/review freshness, or required-review trust; preserve fail-closed behavior when scope actually changes; keep diagnostics clear about whether invalidation was caused by agent workflow error or navigator ordering; add regressions for late startup/preflight refresh after review-context preparation and for legitimate scope-change invalidation. |
| T-585 | 🟦 TODO | P3 | init/project-command-discovery | Prevent `init` from seeding Node/npm defaults into non-Node or unknown-stack projects | gpt-5.4 | 2026-05-19 | balanced | Operator report: a low-capability agent bypassed the orchestrator and ran `init` in a Java project; the workspace was rematerialized with Node-oriented template defaults and full-suite validation tried `npm test` instead of the Gradle command, making later gates invalid. Fix setup/init/project discovery so unknown/custom or non-Node stacks do not get a misleading Node template command. Prefer explicit project command discovery/confirmation or an unconfigured sentinel over defaulting to `npm test`; preserve self-hosted orchestrator repo behavior where `npm test` is valid. Guardrails: do not break Node project setup, do not overwrite user-authored commands, do not weaken manifest/layout verification, and add regressions for Java/Gradle, unknown stack, existing command preservation, and source-checkout orchestrator defaults. |
| T-547-2-F6 | 🟦 TODO | P3 | workflow/workflow-set-confirmation-guidance-constant                                                                              | Extract shared guidance for workflow set operator-confirmation command text                                                                                                                                              | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-2. Review type: refactor. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-2-refactor.md. Original finding: `src/cli/commands/workflow-command.ts` and related help/gate text now repeat the `--operator-confirmed yes --operator-confirmed-at-utc "<ISO-8601 timestamp>"` guidance in several literals. This is acceptable for now, but a small shared formatter/constant would reduce future drift risk. Justification: This task is scoped to enforcement and focused guidance; centralizing all guidance literals is a cleanup refactor that would broaden the main task. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: refactor-only; preserve exact command semantics and validation behavior. |
| T-547-2-F7 | 🟦 TODO | P4 | tests/workflow-set-test-confirmation-helper-reuse                                                                                 | Reuse workflow set confirmation test helpers across updated suites where practical                                                                                                                                       | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-2. Review type: refactor. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-2-refactor.md. Original finding: Test helper reuse is partial: `tests/node/cli/commands/workflow.test.ts` adds `buildOperatorConfirmationArgs()`, while other updated tests still inline the same arg pair. Justification: The inlined usages are isolated in separate test files and keep this fix surgical after the full-suite failure. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: test-maintenance only; avoid broad fixture churn. |
| T-547-2-F9 | 🟦 TODO | P3 | workflow/workflow-set-guidance-drift-audit                                                                                        | Audit policy text drift across workflow set help and onboarding surfaces                                                                                                                                                 | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-2. Review type: refactor. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-2-refactor.md. Original finding: [follow-up] Policy text and enforcement currently match in the reviewed scope, but this contract is now spread across command help, onboarding guidance, and gate messages, which increases long-term consistency maintenance cost. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: documentation/help consistency only; do not change lifecycle behavior without a separate task. |
| T-547-2-F10 | 🟦 TODO | P3 | workflow/workflow-set-downstream-automation-alignment                                                                             | Check downstream automation and docs for the stricter workflow set contract                                                                                                                                              | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-2. Review type: api. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-2-api.md. Original finding: [follow-up] `workflow set` is now intentionally stricter and will break older automation that mutates config without `--operator-confirmed yes --operator-confirmed-at-utc "<ISO-8601 timestamp>"`. This is expected for T-547-2, but downstream scripts/docs outside this scoped file set may still need alignment to avoid operational friction. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: compatibility audit only; do not relax the new confirmation requirement. |
| T-547-1-F1 | 🟦 TODO | P3 | workflow/operator-confirmed-next-step-hardening                                                                                   | Consider timestamp value guidance for protected task-mode recovery commands                                                                                                                                              | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-1. Review type: code. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-1-code.md. Original finding: [follow-up] Low: `next-step` recovery commands intentionally omit concrete `--operator-confirmed-at-utc` value (tests assert this), so execution still depends on operator supplying a fresh timestamp manually; messaging does call this out. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: hardening/UX only; do not let agents self-approve protected work or auto-fill operator approval evidence. |
| T-547-1-F6 | 🟦 TODO | P3 | tests/operator-confirmed-future-timestamp                                                                                         | Add explicit future timestamp regression for protected task-mode approvals                                                                                                                                               | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-1. Review type: test. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-1-test.md. Original finding: [follow-up] Small residual risk: no explicit new assertion for future-dated `--operator-confirmed-at-utc` in the shown diff. Validation exists in code path, but this exact edge is not visibly covered by the newly added tests in this review scope. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: focused test-only follow-up; do not change task-mode confirmation behavior unless the test exposes a real bug. |
| T-547-1-1-F1 | 🟦 TODO | P4 | workflow/operator-confirmed-restart-help-clarity                                                                                  | Clarify conditional approval flags in restart-coherent-cycle help                                                                                                                                                        | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-1-1. Review type: code. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-1-1-code.md. Original finding: Follow-up: `restart-coherent-cycle` help text now shows `--operator-confirmed` and `--operator-confirmed-at-utc` unconditionally, but runtime enforcement is conditional. Consider clarifying help text to avoid user confusion. Justification: Non-blocking UX clarification; runtime enforcement remains fail-closed for protected re-entry. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: UX/help only; do not weaken protected task-mode confirmation. |
| T-547-1-1-F3 | 🟦 TODO | P3 | workflow/operator-confirmed-literal-yes                                                                                           | Evaluate strict literal `yes` parsing for operator confirmations                                                                                                                                                         | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-1-1. Review type: security. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-1-1-security.md. Original finding: Follow-up: `parseOperatorConfirmationYes` currently accepts case/whitespace variants such as `YES` and ` yes ` while error text says "exact value `yes`". Tightening to strict literal match would remove ambiguity for automation/policy enforcement. Justification: Non-blocking hardening; current parsing still requires affirmative operator intent and freshness validation. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: security hardening only; preserve fail-closed missing/stale timestamp behavior. |
| T-547-1-1-F4 | 🟦 TODO | P3 | tests/operator-confirmed-restart-negative                                                                                         | Add restart-coherent-cycle negative approval coverage                                                                                                                                                                    | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-1-1. Review type: security. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-1-1-security.md. Original finding: Follow-up: Add an explicit negative test for `restart-coherent-cycle` confirmation freshness/absence, since this diff updates CLI surface/docs for that gate but does not show direct validation assertions for that path. Justification: Non-blocking coverage improvement; runtime validation is delegated through protected `runEnterTaskModeCommand`. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: test-only unless a real bug is exposed; do not auto-fill operator approval evidence. |
| T-547-1-1-F6 | 🟦 TODO | P3 | tests/operator-confirmed-future-skew                                                                                              | Add task-mode future timestamp rejection coverage                                                                                                                                                                        | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-1-1. Review type: test. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-1-1-test.md. Original finding: Follow-up: Add a `task-mode` test for future-skew timestamp rejection (`--operator-confirmed-at-utc` in the future) to mirror the new guard in `src/core/operator-confirmation.ts`. Justification: Non-blocking coverage hardening; the guard exists in the shared validator. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: focused test-only follow-up. |
| T-547-1-1-F7 | 🟦 TODO | P3 | tests/operator-confirmed-invalid-timestamp                                                                                        | Add task-mode invalid timestamp format coverage                                                                                                                                                                          | gpt-5.4 | 2026-05-15 | balanced | Parent task: T-547-1-1. Review type: test. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-547-1-1-test.md. Original finding: Follow-up: Add a `task-mode` test for invalid ISO timestamp format rejection path (`--operator-confirmed-at-utc must be a valid ISO-8601 timestamp.`) so the shared parser behavior is explicitly locked for this command surface. Justification: Non-blocking coverage hardening; current command surface already delegates to the shared validator. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: focused test-only follow-up. |
| T-096 | 🟦 TODO | P4 | quality/gates-test-parallel-/куisolation                                                                                          | Investigate and reduce `concurrency: false` reliance in `tests/node/cli/commands/gates.test.ts` by fixing hidden shared state or fixture cross-talk where practical                                                      | gpt-5.4 | 2026-04-16 | balanced | Live `T-069` showed a narrow integration-test flake: several new `restart-review-cycle` regressions passed in isolation but only became stable under grouped execution after being marked `concurrency: false`. Production gates and the full orchestration cycle passed, so this is not evidence of a runtime defect, but it does indicate hidden shared process state, fixture ordering sensitivity, or harness-level cross-talk in `gates.test.ts`. Investigate the root cause, document which cases are inherently serial integration flows, and remove `concurrency: false` only where true parallel safety can be restored without making the suite brittle. Guardrails: test harness only; do not change gate production behavior, restart-review-cycle semantics, lifecycle policy, review routing, or command output contracts. Dumb-agent constraint: only touch `tests/node/cli/commands/gates.test.ts` and local test helpers/fixtures unless a very small shared test helper already owns the leaking state; if isolation requires production changes or broad fixture rewrites, stop and report findings instead of continuing. |
| T-309 | 🟦 TODO | P2 | test/full-suite-sharded-runner                                                                                                    | Add a CI-safe prepared-build and sharded full-suite runner to reduce wall-clock time without skipping coverage                                                                                                           | gpt-5.4 | 2026-04-30 | balanced | Full `npm test` currently rebuilds script artifacts and then runs every compiled Node test through one `node --test` invocation, which makes mandatory full-suite gates slow and tempts agents to avoid them. Add an opt-in prepared-build/sharded runner that builds scripts once, partitions compiled test files into deterministic shards, runs safe shards in parallel, and keeps known serial or lock-heavy suites in a single serial bucket. Preserve `npm test` and current CI behavior as the default until the new path proves stable; add a CI-safe command or env flag for controlled enablement, union/no-duplicate shard validation, deterministic aggregated failure reporting, Windows-safe temp/build handling, and regression coverage proving no test files are skipped. Guardrails: do not relax assertions or remove `concurrency: false` as part of this task; do not run multiple wrappers that race on `.scripts-build`; keep the old serial runner as fallback; fail closed on unknown shard classification; and document how CI can opt in after a green comparison run. |
| T-232 | 🟦 TODO | P2 | workflow/auditable-intermediate-command-compaction                                                                                | Add an auditable wrapper for intermediate test and typecheck command runs so compacted shell output can save and report token economy without pretending arbitrary ad-hoc commands were compacted                        | gpt-5.4 | 2026-04-23 | balanced | Today intermediate shell commands run during implementation may save tokens in practice when the agent uses compact output or only inspects the tail, but that savings is not reliably measurable because ad-hoc `shell_command` runs do not emit orchestrator `output_telemetry` or durable raw-output artifacts. Introduce an explicit guarded command-runner contract for selected intermediate validation flows such as targeted tests, `npm test -- ...`, `node --test ...`, typecheck, or similar bounded verification commands: the runner should preserve full stdout/stderr in an artifact, expose only compact summary or bounded failure chunks to the agent by default, and emit auditable token-economy telemetry that `task-events-summary`, `task-audit-summary`, and `stats` can aggregate. Guardrails: do not claim savings for commands whose full output was actually surfaced to the agent; do not silently treat every arbitrary shell command as token-economy eligible; keep the feature opt-in and source-specific rather than magic global interception; preserve direct raw-shell access for cases that need it; and add regressions proving both counted wrapper runs and intentionally uncounted plain shell runs. |
| T-234 | 🟦 TODO | P2 | ux/output-compaction-reporting-contract-source                                                                                    | Establish one canonical output-compaction reporting contract so gate summaries, stats, task-audit-summary, final-closeout artifacts, and skill or rule guidance stop drifting on wording and metric semantics            | gpt-5.4 | 2026-04-25 | balanced | Recent char-first reporting changes touched runtime gate output, `stats`, `task-events-summary`, `task-audit-summary`, final-closeout rendering, and skill or rule wording in multiple places. Today those surfaces still rely on hand-synced strings and partial helper reuse, so future tweaks to labels, units, or token-note wording will drift again. A live comparison showed real model usage around 12.5k tokens for a simple `npm test` prompt while Garda-facing closeout wording could imply 120k+ "tokens" by mixing suppressed-output estimates with actual model token consumption. Introduce one canonical typed output-compaction contract that defines metric names, primary unit (`chars`), optional secondary estimate explicitly labeled as `suppressed output estimate`, label mapping for sources such as compile/full-suite/review-context output, and any generated or validated wording fragments used across runtime and docs. Guardrails: do not print estimated "saved tokens" to the user-facing final closeout as if they were model tokens; keep model-token counts separate from suppressed-output accounting; put per-step estimates in audit/stats artifacts only unless explicitly requested; do not overclaim savings when the metric really means output not surfaced to the agent; avoid duplicating canonical wording between live/template docs and runtime code; and keep renderer-specific wrappers thin so they consume the same contract instead of rephrasing the metric semantics by hand. |
| T-217 | 🟦 TODO | P2 | observability/versioned-task-events-schema                                                                                        | Promote `runtime/task-events/*.jsonl` to a versioned public event schema with compatibility guarantees and a compact latest-cycle consumer contract                                                                      | gpt-5.4 | 2026-04-22 | balanced | Task events are already a core source of truth for audits, summaries, and recovery, but today external or future consumers still depend too much on implicit field knowledge and best-effort parsing. Define a versioned public task-event schema with explicit required fields such as schema version, task id, profile, gate or phase, timestamps or duration, result, source-of-truth or bundle identity, and evidence references, then align compact latest-cycle consumers to that contract. Guardrails: keep append cost cheap enough for hot workflow paths; preserve backward readability for older event lines where practical; do not require an HTTP control plane just to consume runtime state; and avoid forcing every internal experimental field into the public stable surface. |
| T-297 | 🟦 TODO | P5 | refactor/task-events-summary-module-decomposition                                                                                 | Split `src/gates/task-events-summary.ts` into parsing, noise classification, aggregation, and rendering helpers                                                                                                          | gpt-5.4 | 2026-04-29 | balanced | The style pass found `src/gates/task-events-summary.ts` above 1200 lines with parsing, noisy-command classification, aggregation, token economy, and rendering logic in one module. Decompose it into focused helpers/modules while preserving the command output contract. Guardrails: no behavior change; keep machine-readable JSON fields stable; add focused regression coverage for noisy-command grouping and latest-cycle summaries before moving logic. |
| T-498 | 🟦 TODO | P3 | ux/task-dates-created-metadata                                                                                                    | Add optional task creation date metadata without adding TASK.md columns                                                                                                                                                  | gpt-5.4 | 2026-05-09 | balanced | Add a compact optional `dates(created=YYYY-MM-DD)` marker in the existing `Notes` field for new or edited tasks, without adding `Created`/`Closed` columns and without migrating old rows. Teach task-specific human stats, such as `garda stats T-001` and `garda task T-001 stats`, to show `Created` from `dates(created=...)` when present, while `Started` and `Closed` come from task-events fallbacks such as first task event, task-mode entry, `COMPLETION_GATE_PASSED`, or DONE/finalization evidence. Guardrails: additive UX/stats only; do not restructure the Active Queue table, add columns, make `dates(...)` mandatory, hand-edit old tasks in bulk, write `closed` into TASK.md, change lifecycle status semantics, event schemas, task completion logic, aggregate stats totals, or existing started/closed fallback behavior. Minimal tests: parse present/absent `dates(created=...)`, task stats shows created from metadata, started/closed from events, and legacy rows without dates remain valid. Dumb-agent constraint: keep edits narrow to task metadata parsing, stats rendering, and focused tests; if a solution needs table migration, event schema changes, or lifecycle changes, stop and report a separate design task. |
| T-269 | 🟦 TODO | P2 | workflow/doc-impact-parameter-guidance                                                                                            | Make doc-impact-gate reject near-miss enum values with actionable docs-updated guidance and next-step command repair                                                                                                     | gpt-5.4 | 2026-04-27 | balanced | A live task used `DOC_UPDATES_COMPLETE` instead of the valid `DOCS_UPDATED` doc-impact decision and initially omitted the required `--docs-updated` list after changing `CHANGELOG.md`. Tighten CLI validation, help, and `next-step` repair text so invalid enum values are rejected with the nearest valid decision, and `DOCS_UPDATED` requires explicit updated-doc paths. Guardrails: keep existing machine-readable doc-impact artifact fields stable; do not infer docs updates from git diff without operator confirmation; make `NO_DOC_UPDATES` stay valid only when no docs are required; include examples for `CHANGELOG.md`; and add regressions for invalid enum, missing docs-updated list, and a valid changelog update. |
| T-267 | 🟦 TODO | P2 | ux/final-closeout-depth-fidelity                                                                                                  | Make final closeout report the current preflight effective depth and escalation reason instead of stale task-mode depth                                                                                                  | gpt-5.4 | 2026-04-27 | balanced | Live `T-259` final closeout reported `requested_depth: 1` and `effective_depth: 1` from task-mode even though the current preflight for the same completion cycle showed `effective_depth: 2` with escalation reason `full_path_minimum_depth_2, test_review_required`. Align `task-audit-summary` with the existing stats behavior by preferring current preflight `budget_forecast` / `depth_escalation` for final closeout depth, while retaining task-mode depth as fallback only. Guardrails: keep historical completed task summaries stable when preflight is missing; include escalation reason where available; do not mutate task-mode artifacts; and add regressions where task-mode depth differs from preflight effective depth. |
| T-106 | 🟦 TODO | P2 | workflow/final-closeout-additional-issues                                                                                         | Add an optional final-closeout section for non-blocking out-of-scope issues discovered during task execution and require the agent to suggest follow-up task creation when such issues exist                             | gpt-5.4 | 2026-04-17 | balanced | During implementation or review the agent may notice real side issues that do not block the current task and should not silently disappear from the final report. Extend the canonical final closeout artifact and user report contract with an optional `Additionally found issues` section that appears only when such issues were actually found, and have the agent explicitly propose creating follow-up tasks for them. Guardrails: do not use this section for blocking or in-scope findings that must instead be fixed now or force `BLOCKED`; keep the section empty or omitted when nothing extra was found; preserve per-issue severity and rationale where helpful; and keep the source of truth in audited closeout artifacts instead of ad-hoc chat prose. |
| T-463 | 🟦 TODO | P5 | docs/deferred-followup-canonical-taskmd-guidance                                                                                  | Document canonical TASK.md formatting requirements for strict deferred follow-up rows if operator confusion appears                                                                                                      | gpt-5.4 | 2026-05-08 | balanced | Deferred from T-459 code review artifact garda-agent-orchestrator/runtime/reviews/T-459-code.md. Original finding: [follow-up] Low. The parser intentionally fails closed for malformed tables or unescaped pipe characters in Notes, so operators must preserve canonical TASK.md markdown when materializing follow-ups. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: documentation or operator guidance only; do not loosen strict deferred follow-up matching, review trust, receipt schemas, completion eligibility, or canonical Active Queue parsing. Dumb-agent constraint: docs-only task; do not edit `src/**`, `tests/**`, runtime artifacts, schemas, prompts, workflow config, or TASK.md parser behavior. Keep examples small and clearly marked as formatting guidance; if parser behavior appears wrong, stop and create/report a separate implementation task. |
| T-358 | 🟦 TODO | P2 | workflow/strict-task-decomposition-preflight                                                                                      | Add a prompt-first strict-task decomposition decision before large strict tasks start implementation                                                                                                                     | gpt-5.4 | 2026-05-02 | strict | Implement the updated intent in `docs_local/strict-task-decomposition-plan-ru.md`: before editing files for a risky or umbrella-shaped `strict` task, require a compact decomposition decision that says whether the task is atomic, can stay a single strict cycle, or must be split into sequential strict child tasks. Existing scope-budget and review-cycle guards remain authoritative: an `atomic` or `single-cycle` decision is valid only while actual scope and review pressure still match it; if a later guard materializes `SPLIT_REQUIRED`, `next-step` must treat the earlier decision as stale or overridden and require split. Scope this as prompt/rule/next-step guidance plus a small auditable decision artifact or event first; gate/runtime changes are in scope only where needed to make `next-step` refuse ordinary continuation after an explicit `split-required` decision or to validate the current decision artifact, not to rewrite compile/review/completion gates. The decision must record reason, scope risk, expected review types, atomicity constraints, and proposed parent-derived child sequence such as `T-358-1`, `T-358-2`. Child tasks created from a `strict` parent must also be `strict`; split only reduces diff size and review scope per task cycle, not review rigor. Guardrails: preserve existing `SPLIT_REQUIRED`, scope-budget, review-cycle, and parent-derived ID semantics; do not create legacy unrelated numeric child IDs for new splits; do not leave the parent as a duplicate executable task when fully replaced; do not let split bypass security/API/test/refactor review; add regressions for atomic strict tasks, single-cycle strict tasks, guard override after stale single-cycle decisions, split-required umbrella tasks, strict child profile preservation, stale/missing decision evidence, and full-suite failure triage before test review. |
| T-240 | 🟩 DONE | P1 | security/runtime-secret-redaction-coverage                                                                                        | Expand and test secret redaction across events, review artifacts, command output, compile logs, and full-suite validation evidence                                                                                       | gpt-5.4 | 2026-04-24 | strict | Split into child tasks `T-240-1` and `T-240-2`; do not continue the parent. Runtime events and review artifacts can preserve command output and failure details, so redaction must be validated beyond the current basic environment and path masking paths. Audit every place where event details, review artifacts, compile output, preflight details, full-suite logs, and final reports can capture stdout or stderr. Add test fixtures for .env values, GitHub tokens, npm tokens, Authorization headers, database URLs, private keys, stack traces, and multiline secrets. Guardrails: do not destroy useful diagnostics when masking; make redaction deterministic; keep raw artifacts safe by default; and explicitly document any surfaces that intentionally remain unredacted for local-only debugging. |
| T-240-1 | 🟩 DONE | P1 | security/runtime-secret-redaction-core-surfaces | Redact secrets in core runtime events, review artifacts, command output, compile output, and final closeout surfaces                                                                                                     | gpt-5.4 | 2026-05-18 | strict | Parent task: T-240. Split child 1/2. Scope: `src/core/redaction.ts`, `src/cli/commands/gates-subprocess.ts`, `src/cli/commands/gates-formatter.ts`, `src/gate-runtime/review-artifacts.ts`, `src/gate-runtime/task-events-io.ts`, `src/gates/task-audit-summary.ts`, and focused tests in `tests/node/core/redaction.test.ts`, `tests/node/cli/commands/gates.test.ts`, `tests/node/gate-runtime/review-artifacts.test.ts`, `tests/node/gate-runtime/task-events-append.test.ts`, `tests/node/gates/task-audit-summary-closeout.test.ts`. Guardrails: preserve diagnostic utility and token telemetry/verdict metadata; cover multiline env-style secrets split across output lines and compile command headers. |
| T-240-2 | 🟩 DONE | P1 | security/full-suite-secret-redaction-surfaces | Redact secrets in full-suite validation command, output log, JSON artifact, and duration history surfaces                                                                                                                | gpt-5.4 | 2026-05-18 | strict | Parent task: T-240. Split child 2/2. Scope: `src/cli/commands/gate-flows/full-suite-validation-flow.ts`, `src/gates/full-suite-validation.ts`, and `tests/node/gates/full-suite-validation.test.ts`. Guardrails: raw command remains usable for execution and config signature, but formatted output, artifacts, events, and duration history must not expose inline secrets. |
| T-335-F1 | 🟨 IN_PROGRESS | P5 | tests/delegation-trust-on-disk-spoofing-coverage                                                                                  | Add integration-style spoofing coverage for delegation trust runtime classification                                                                                                                                      | gpt-5.4 | 2026-05-14 | balanced | Deferred from T-335 security review artifact garda-agent-orchestrator/runtime/reviews/T-335-security.md. Original finding: Low: Add one integration-style spoofing test that drives `resolveDelegatedLauncherTrustEvidence(...)` from an on-disk spoofed runtime shape (not only `buildDelegationTrustEvidence(...)` with injected fields) to prove `runtime_kind` cannot be misclassified into an allowed delegation path. Justification: current tests cover the fail-closed decision logic directly, but an end-to-end classification regression would better protect the trust boundary against future classifier drift. Justification: Preserved from raw reviewer output during PASS review normalization. Parent task: T-335. Review type: security. Source artifact: garda-agent-orchestrator/runtime/reviews/T-335-security.md. Guardrails: tests only; do not change delegation behavior unless the new test exposes a real current bug, then stop and create/report a separate implementation task. |
| T-335-F2 | 🟦 TODO | P5 | security/delegation-runtime-provenance-hardening                                                                                  | Explore stronger runtime provenance evidence beyond local package-shape heuristics                                                                                                                                       | gpt-5.4 | 2026-05-14 | balanced | Deferred from T-335 security review artifact garda-agent-orchestrator/runtime/reviews/T-335-security.md. Original finding: [follow-up] Delegation trust still relies on local filesystem/package-shape identity (recognized package + runtime-shape heuristics) rather than cryptographic provenance. That is consistent with the current local-workspace trust model, but a compromised local workspace can still present a trusted-looking runtime. Justification: Preserved from raw reviewer output during PASS review normalization. Parent task: T-335. Review type: security. Source artifact: garda-agent-orchestrator/runtime/reviews/T-335-security.md. Guardrails: design or narrow hardening only; do not silently require signatures or external services for ordinary local source-checkout workflows without an explicit policy decision. |
| T-335-F3 | 🟦 TODO | P5 | refactor/delegation-trust-decision-helper                                                                                         | Reduce duplication in delegation trust mandatory-review decision construction                                                                                                                                            | gpt-5.4 | 2026-05-14 | balanced | Deferred from T-335 refactor review artifact garda-agent-orchestrator/runtime/reviews/T-335-refactor.md. Original finding: Consider extracting a small constructor/helper for repeated `mandatory_review_delegation` fields (`requires_provider_launch_attestation: true` plus repeated trust/decision scaffolding) inside `buildDelegationTrustEvidence` to reduce branch duplication and future drift risk. Justification: This is maintainability-only; current behavior is consistent and covered by added trust-model tests. Justification: Preserved from raw reviewer output during PASS review normalization. Parent task: T-335. Review type: refactor. Source artifact: garda-agent-orchestrator/runtime/reviews/T-335-refactor.md. Guardrails: refactor only; preserve existing trust decisions and tests. |
| T-335-F4 | 🟦 TODO | P5 | tests/delegation-trust-wrapper-branch-locks                                                                                       | Add wrapper-level branch-lock tests for deployed-bundle and unrecognized packaged runtimes                                                                                                                               | gpt-5.4 | 2026-05-14 | balanced | Deferred from T-335 test review artifact garda-agent-orchestrator/runtime/reviews/T-335-test.md. Original finding: Add two narrow branch-lock tests around `resolveDelegatedLauncherTrustEvidence`: 1) current runtime is a recognized deployed bundle (not `node_modules`), 2) current runtime is under `node_modules` with unrecognized package name. Justification: these are policy-relevant edge branches in `buildDelegationTrustEvidence`; current coverage is strong but does not assert those exact wrapper-level paths yet. Justification: Preserved from raw reviewer output during PASS review normalization. Parent task: T-335. Review type: test. Source artifact: garda-agent-orchestrator/runtime/reviews/T-335-test.md. Guardrails: tests only; do not change delegation behavior unless the new tests expose a real current bug, then stop and create/report a separate implementation task. |
| T-336 | 🟦 TODO | P1 | security/no-delegate-mode-and-env                                                                                                 | Add fail-safe no-delegate mode and environment fallback for unsafe delegation contexts                                                                                                                                   | gpt-5.4 | 2026-05-02 | strict | Split from T-214. Implement a hard no-delegate mode such as --no-delegate plus an env fallback such as GARDA_NO_DELEGATE=1, and make delegation attempts refuse with auditable diagnostics when the mode is active or required trust is unavailable. Guardrails: distinguish ordinary subprocess execution from reviewer or implementation delegation; do not fabricate review completion when delegation is disabled; keep remediation wording operator-facing. |
| T-337 | 🟦 TODO | P1 | security/delegation-target-validation                                                                                             | Validate candidate delegation targets with repo-root, package, and path-containment checks                                                                                                                               | gpt-5.4 | 2026-05-02 | strict | Split from T-214. Add validation for expected package name, plausible package metadata, target-root containment, and mismatch refusal before using a checkout as delegated implementation or reviewer runtime. Guardrails: fail closed on ambiguous roots; avoid global-process magical trust; keep deterministic tests without relying on real external repositories. |
| T-338 | 🟦 TODO | P2 | docs/delegation-trust-diagnostics                                                                                                 | Surface effective delegation trust state in diagnostics and operator documentation                                                                                                                                       | gpt-5.4 | 2026-05-02 | balanced | Split from T-214. Update diagnostics and docs so operators can see the effective delegation policy, runtime source, delegated path, no-delegate state, and refusal reason. Guardrails: documentation and diagnostics must match T-335 through T-337; do not add new trust behavior in this task; avoid implying disabled delegation satisfies mandatory review. |
| T-239 | 🟦 TODO | P1 | security/protected-control-plane-strict-mode                                                                                      | Strengthen protected control-plane scanning with an explicit symlink policy, optional no-cache strict mode, and clearer cache trust boundaries                                                                           | gpt-5.4 | 2026-04-24 | strict | Protected control-plane hash checks currently depend on performance cache hits and do not make symlink handling an explicit security contract. Define whether symlinks under protected roots are forbidden, hashed as link objects, or resolved only when targets remain inside the repository root. Add a strict mode that can bypass mtime and size cache hits, include stronger cache metadata where available, and document that the cache is a performance feature rather than a security boundary. Guardrails: keep normal scans fast by default; preserve current accidental-drift detection behavior; avoid following symlinks outside the repo; and add regressions for symlink, cache-hit, and strict-scan cases. |
| T-241 | 🟦 TODO | P1 | release/update-provenance-and-signing                                                                                             | Add release-update provenance, integrity, and signature policy for self-update paths or explicitly gate self-update behind package-manager-only enterprise mode                                                          | gpt-5.4 | 2026-04-24 | strict | The update trust model already uses allowlists and rejects local sources by default, but a governance tool that updates itself still needs stronger provenance before broad release. Decide whether self-update remains enabled for 1.1.x and, if so, add expected package integrity or checksums, release signature verification where feasible, provenance fields in update reports, and a dry-run-first posture for risky updates. If not, add an enterprise or strict mode that disables self-update and routes operators to package-manager or CI-controlled updates. Guardrails: preserve existing allowlist-first behavior; do not accept arbitrary local or git sources without explicit trust override; keep tests deterministic without requiring live network trust; and document the supported update trust model. |
| T-218 | 🟦 TODO | P2 | security/ci-supply-chain-baseline                                                                                                 | Add a baseline CI security pipeline for secrets scanning, dependency risk, SBOM generation, and update-source policy reporting                                                                                           | gpt-5.4 | 2026-04-23 | strict | Garda installs, updates, and manages local control-plane files, so the repository should have a baseline CI security story that goes beyond ordinary correctness and packaging checks. Add a pragmatic first-pass CI security pipeline covering secrets scanning, dependency or SCA scanning, SBOM generation, and a compact policy report for accepted update sources or transports so release and update trust become more auditable. Also document the recommended required-check set for branch protection or release gating and decide whether high-risk third-party GitHub Actions should stay version-pinned or move to full SHA pinning. Guardrails: keep local development lightweight and avoid making ordinary offline work impossible; define clearly which checks are blocking versus informational; keep outputs deterministic enough for CI triage; and do not pretend a baseline pipeline replaces deeper provenance or release-signing work that may come later. |
| T-221 | 🟦 TODO | P2 | packaging/sourceful-vs-slim-distribution-policy                                                                                   | Decide and enforce whether the published package and deployed bundle intentionally include both `src` and `dist` or move to a slimmer runtime-oriented distribution                                                      | gpt-5.4 | 2026-04-23 | balanced | The current npm package and deploy surface intentionally publish both `src` and `dist`, plus TypeScript config files and templates, but that policy is not yet clearly documented as a product decision. Decide whether Garda should remain an explicitly sourceful distribution or move toward a slimmer production package. If sourceful is intentional, document the rationale, add publish-surface expectations and a pack-smoke contract for the exact allowed file set, and tighten parity expectations between source and runtime. If slim distribution is preferred, remove non-runtime files from package and deployed-bundle surfaces in a controlled way. Guardrails: do not break self-hosted or source-checkout workflows by accident; keep pack/install smoke deterministic; and avoid half-migrated states where package, deploy items, and docs each imply a different distribution model. |
| T-162 | 🟦 TODO | P2 | docs/provider-native-skill-format-contract                                                                                        | Add a canonical provider-native skill and instruction format contract that maps Garda `live/skills` to the real customization surfaces of every supported or planned provider                                            | gpt-5.4 | 2026-04-21 | docs-only | Garda currently treats `garda-agent-orchestrator/live/skills/**` as the canonical internal runtime format, but external providers do not share one native skill contract: GitHub Copilot expects `.github/skills/<skill>/SKILL.md` and custom agent YAML frontmatter, Claude and Qwen have their own `SKILL.md` discovery roots, Gemini relies on `GEMINI.md` plus commands or extensions, Windsurf uses `.windsurf/rules/*.md` and `AGENTS.md`, Junie uses `.junie/AGENTS.md` or guidelines, Codex primarily uses `AGENTS.md`, and planned `Cursor` uses `.cursor/rules/*.mdc` with `AGENTS.md` as a simpler alternative. Add one repo-owned spec or matrix that records, per provider, whether Garda should emit native skills, agents, rules, commands, or only root instructions; where the files must live; what metadata or frontmatter is required; and how runtime discovery works. Guardrails: keep `live/skills` as the only canonical source-of-truth for skill intent, do not hand-maintain duplicated provider instructions, keep `Cursor` aligned with the shared-entrypoint direction from `T-136`, and make the matrix precise enough that install, update, doctor, and tests can derive behavior from it rather than from scattered hardcoded assumptions. |
| T-225 | 🟦 TODO | P2 | workflow/follow-up-issue-capture-registry                                                                                         | Capture non-blocking out-of-scope issues discovered during implementation or review in a durable TASK.md follow-up surface so they can be promoted into real tasks later                                                 | gpt-5.4 | 2026-04-23 | balanced | `T-106` already covers showing additionally found issues in final closeout artifacts, but those findings still disappear too easily after the chat ends. Add a durable local capture surface in `TASK.md` for non-blocking discovered issues, for example a dedicated bottom table or registry section keyed by source task, summary, scope, severity, and follow-up disposition. Guardrails: do not use this registry for blocking or in-scope defects that must be fixed now; keep the main active queue readable instead of turning every task row into a dumping ground; require enough structure that entries can be promoted into first-class tasks later; and keep the feature local to orchestrator control-plane files instead of pretending these follow-ups are already committed roadmap items. |
| T-141 | 🟦 TODO | P3 | ux/partial-task-cycle-status-reconciliation                                                                                       | Reconcile `status`, `why-blocked`, and `task-audit-summary` when a partial or diagnostic task cycle exists so workspace readiness and stalled-task signals cannot disagree                                               | gpt-5.4 | 2026-04-19 | balanced | The same diagnostic `T-137` probe left `status --compact` reporting the workspace as `ready` while `status why-blocked` showed `T-137` stalled and `task-audit-summary` rendered the task `BLOCKED` because review artifacts were absent. Align these surfaces so incomplete or diagnostic task cycles are represented consistently without hiding real blockers or over-reporting the workspace as healthy. Guardrails: keep compact status cheap, preserve high-signal remediation in `why-blocked`, and make diagnostic or abandoned task cycles explicit rather than relying on operators to reconcile three different interpretations by hand. |
| T-169 | 🟦 TODO | P2 | ux/update-availability-notification-flow                                                                                          | Add a non-blocking update-availability notification flow that feeds cached upgrade hints and trust metadata into agent-authored session-start and final-report messages                                                  | gpt-5.4 | 2026-04-21 | balanced | Garda already has manual `check-update` and `update` flows, but it still lacks a first-class way to notify operators that an update exists unless they remember to check on their own. Add an update-availability notification mechanism that opportunistically performs a throttled automatic version check on selected user-invoked CLI entrypoints, caches the last successful check result locally, and exposes a short non-blocking notice such as current version, available version, the exact next command to update or inspect the release, and compact trust metadata describing the effective update source, transport, and policy decision. The notice should be consumable by the agent so it can mention it once near the beginning of a user session when the cache says a newer version exists, and again in the deterministic final report block after onboarding or task completion. Guardrails: do not auto-apply updates; prefer an opportunistic cached check over a background daemon or long-running scheduler; make the cadence configurable or suppressible so users can opt out or snooze repeated notices; keep network failures silent or low-noise instead of degrading ordinary commands; avoid checking on every gate or hot path; keep the surfaced recommendation consistent with the existing `check-update`, `check-update --apply`, and `update` command semantics; and do not assume Garda can inject text into provider chats without the agent explicitly rendering it. |
| T-166 | 🟦 TODO | P2 | workflow/shared-entrypoint-provider-model                                                                                         | Make the canonical provider model support multiple providers sharing one root entrypoint file explicitly, starting with `Cursor` and `Codex` on `AGENTS.md`                                                              | gpt-5.4 | 2026-04-21 | balanced | The current provider model assumes that each provider owns a unique canonical entrypoint file, but planned `Cursor` support and the growing provider-native surface matrix require explicit support for providers that intentionally share the same root entrypoint while still differing in runtime identity, reviewer routing, and native adapter emission. Introduce shared-entrypoint support in the provider registry, setup, materialization, diagnostics, and tests so providers can reuse `AGENTS.md` without collapsing into one indistinguishable provider identity. Guardrails: keep provider identity explicit in gate artifacts and materialization logic, do not infer `Cursor` implicitly from `Codex` or vice versa, preserve existing behavior for unique-entrypoint providers, and make any shared-entrypoint resolution deterministic and testable instead of relying on ad-hoc file-name heuristics. |
| T-261 | 🟦 TODO | P1 | workflow/provider-identity-aliases                                                                                                | Add canonical provider identity aliases so Copilot and other providers normalize to Garda's provider registry names                                                                                                      | gpt-5.4 | 2026-04-27 | balanced | Copilot agents repeatedly report or record their provider identity using names that do not exactly match Garda's canonical provider registry, which causes runtime identity, provider bridge routing, diagnostics, and review capability checks to drift or fail even though the provider is recognizable. Add a first-class provider alias map to the provider registry and use it anywhere provider identity is parsed or validated, including task-mode provider input, environment/provider detection, routing diagnostics, provider compliance, materialization checks, and tests. Guardrails: canonical provider ids remain stable; aliases are accepted only for known providers and normalized before persistence; diagnostics should show both raw and canonical identity when useful; do not silently invent a new provider from unknown strings; include GitHub Copilot spelling variants such as `copilot`, `github-copilot`, `GitHubCopilot`, and provider-agent-specific labels observed in live runs; and add regressions proving alias input lands on the canonical Garda provider id. |
| T-163 | 🟦 TODO | P2 | materialization/provider-native-skill-emission                                                                                    | Materialize canonical Garda skills into true provider-native skill files for providers that actually support discoverable skill directories instead of relying only on bridge prose                                      | gpt-5.4 | 2026-04-21 | balanced | Bridge profiles that merely point agents at `garda-agent-orchestrator/live/skills/**` are not enough for providers such as GitHub Copilot, Claude, and Qwen to use their native skill-loading mechanisms. Add materialization from canonical live skills into provider-native skill roots such as `.github/skills/<skill>/SKILL.md`, `.claude/skills/<skill>/SKILL.md`, and `.qwen/skills/<skill>/SKILL.md`, with provider-specific shaping for required frontmatter, naming, discovery metadata, and references. Guardrails: the generated provider files must always derive from `live/skills` manifests instead of becoming hand-edited forks; install, update, remove, and validate must keep the generated surfaces in sync; Copilot formatting must satisfy its strict YAML requirements; and the implementation must not pretend that Gemini, Windsurf, Junie, Codex, or Cursor consume the same skill format when they do not. |
| T-164 | 🟦 TODO | P2 | materialization/provider-native-non-skill-adapters                                                                                | Add provider-native adapters for providers whose real customization surface is rules, guidelines, commands, or shared entrypoints rather than `SKILL.md` directories                                                     | gpt-5.4 | 2026-04-21 | balanced | Several providers in Garda's target set do not primarily consume `SKILL.md` directories even though Garda has canonical live skills internally. Add explicit provider-native projection rules for Codex, Gemini, Windsurf, Junie, and planned Cursor so orchestration and review skills influence the files those providers actually read, such as `AGENTS.md`, `GEMINI.md`, `.gemini/commands/*.toml`, `.windsurf/rules/*.md`, `.junie/AGENTS.md` or guidelines, and later `.cursor/rules/*.mdc` where appropriate. Guardrails: keep the provider outputs generated from canonical live skill intent instead of inventing a second source of truth; do not add a dedicated Cursor bridge unless `T-136` or upstream provider requirements force one; preserve the current shared `AGENTS.md` contract for Codex and planned Cursor where it remains the correct surface; and make provider-specific projection selectable at init so inactive providers do not accumulate dead control-plane files. |
| T-165 | 🟦 TODO | P2 | validation/provider-native-skill-surface-audit                                                                                    | Teach install, update, doctor, and validation commands to detect missing, stale, malformed, or inactive-provider-native skill surfaces and explain exactly what Garda generated versus what the provider can really use  | gpt-5.4 | 2026-04-21 | balanced | Once Garda starts differentiating between canonical live skills and provider-native emitted surfaces, the workflow needs first-class diagnostics so operators can tell whether a provider will actually honor those skills. Extend validation and operator-facing diagnostics to audit generated provider-native skill or rule surfaces, report missing or malformed YAML or metadata where providers require it, flag drift between `live/skills` and emitted files, and distinguish active-provider generation from intentionally absent inactive-provider outputs. Guardrails: diagnostics must stay high-signal and provider-specific instead of collapsing into one vague `skills broken` error, must not require opening every full optional `SKILL.md` on every run, and should make it obvious why a provider like Copilot or future Cursor is or is not able to use the emitted customization files in the current workspace. |
| T-339 | 🟦 TODO | P2 | workflow/lifecycle-phase-manifest-schema                                                                                          | Create the canonical typed task-lifecycle phase manifest schema                                                                                                                                                          | gpt-5.4 | 2026-05-02 | strict | Split from T-220. Define one typed manifest that can represent ordered lifecycle stages, explicit stage ids, conditionals, mandatory-in-sequence gates, and recommended post-closeout commands. Guardrails: preserve current runtime lifecycle semantics; make TASK_ENTRY and POST_PREFLIGHT rule-pack stages explicit; keep full-suite-validation conditional. |
| T-340 | 🟦 TODO | P2 | workflow/lifecycle-manifest-validation                                                                                            | Validate existing docs, skills, and workflow surfaces against the lifecycle manifest                                                                                                                                     | gpt-5.4 | 2026-05-02 | balanced | Split from T-220. Add validation that README, HOW_TO, start-task router, skills, and rule docs do not drift from the canonical lifecycle manifest. Guardrails: do not introduce a second prose-only canonical list; keep diagnostics precise enough to name the stale surface and missing or extra phase. |
| T-341 | 🟦 TODO | P2 | workflow/lifecycle-manifest-runtime-consumers                                                                                     | Make runtime and next-step consumers use the lifecycle phase manifest where order matters                                                                                                                                | gpt-5.4 | 2026-05-02 | strict | Split from T-220. Wire manifest consumers into runtime or next-step paths that currently hand-maintain gate order, conditionality, or closeout semantics. Guardrails: preserve existing command behavior and exit codes; do not generate opaque UX from raw schema dumps; keep mandatory order separate from recommended audits. |
| T-342 | 🟦 TODO | P2 | docs/lifecycle-manifest-generated-surfaces                                                                                        | Generate or update human-facing lifecycle docs from the canonical manifest                                                                                                                                               | gpt-5.4 | 2026-05-02 | balanced | Split from T-220. Update human-facing lifecycle sections so they are generated from or validated by the manifest rather than manually rephrased. Guardrails: docs must stay readable; keep provider bridge wording intact; do not overwrite user-owned local docs outside generated or managed surfaces. |
| T-233 | 🟦 TODO | P2 | workflow/declarative-workflow-settings-manifest                                                                                   | Introduce one typed workflow-settings manifest so `workflow-config` defaults, preserve-or-seed merge rules, CLI show/set surfaces, validation, and future toggle settings stop drifting across code paths                | gpt-5.4 | 2026-04-23 | balanced | Workflow settings such as `full_suite_validation` are currently spread across default config seeding, update/setup merge logic, CLI rendering, validation, docs, and tests. That is manageable for one flag, but it will drift as soon as more toggles land. Introduce one typed workflow-settings manifest or schema that defines each setting’s key, default, whether missing values should be seeded, whether explicit repo-local values must be preserved across refresh paths, CLI label/help text, and supported `workflow show/set` behavior. Guardrails: do not replace typed code with one giant unvalidated YAML blob; preserve backward compatibility for older `workflow-config.json` files; keep per-setting help human-readable instead of dumping raw schema; support additive future toggles without forcing init/reinit questions; and ensure materialization/update code, validation, and CLI surfaces consume the same manifest rather than copying defaults or semantics by hand. |
| T-219 | 🟦 TODO | P1 | workflow/per-command-target-root-parity-policy                                                                                    | Define and enforce an explicit per-command source or bundle parity policy for `--target-root` and similar workspace-selection surfaces instead of blocking only a small hardcoded subset                                 | gpt-5.4 | 2026-05-05 | balanced | The dispatcher already resolves `--target-root` and `--bundle-root`, but parity fail-fast currently blocks only `gate`, `agent-init`, `skills`, `profile`, and `workflow` while other workspace-sensitive commands such as `status`, `doctor`, `verify`, `preprompt`, `update`, and `check-update` still bypass that policy. Audit intake from `docs_local/Audit/garda-dev-audit-md/05-parity-guard-mutating-commands.md`: mutating lifecycle commands such as `setup`, `install`, `init`, `reinit`, `update`, `rollback`, and `uninstall` must be parity-guarded by default, while true read-only commands remain explicit. Introduce an explicit per-command parity policy such as `block`, `warn`, or `skip`, apply it consistently in dispatch and diagnostics, and add tests for external-cwd plus explicit-target-root scenarios so commands do not silently inspect the wrong workspace. Guardrails: do not blindly force every command into hard-block mode; preserve discoverable help even when parity blocks execution; keep the policy explicit enough to migrate later into `T-189` command metadata; and make operator-facing output explain why a command was blocked, warned, or allowed. |
| T-107 | 🟦 TODO | P2 | workflow/strict-planning-threshold                                                                                                | Require plan-guided task start for `strict` runs above a defined scope or risk threshold while allowing tiny localized strict tasks to use a lighter planning contract                                                   | gpt-5.4 | 2026-04-17 | balanced | `strict` mode is a natural place to require more up-front planning, but making a full approved plan mandatory for every tiny edit would add bureaucracy without improving outcomes. Define a threshold policy for `strict` execution so multi-file, high-risk, ambiguous, security-sensitive, refactor-heavy, database-heavy, or orchestrator-work tasks must attach an approved plan at task start, while truly tiny localized strict tasks may use a compact planning artifact such as structured success criteria instead. Guardrails: do not let planning become a bypass around mandatory gates, preserve backward compatibility for non-plan-guided runs outside the threshold, keep drift detection tied to the approved plan artifact when present, and make the threshold machine-checkable rather than chat-only. |
| T-078 | 🟦 TODO | P2 | workflow/explicit-success-criteria-contract                                                                                       | Add a mandatory structured success-criteria block before implementation (`goal`, `done_when`, `verification`), with extra `repro` and `fix condition` fields for bugfixes and explicit observable outcome for behavior changes | gpt-5.4 | 2026-04-15 | balanced | Current orchestration captures scope, reviews, and verification after implementation, but it does not force the agent to declare a short machine-checkable target before coding. Add a lifecycle artifact or timeline event that records structured success criteria early enough to guide compile, review, and doc-impact flow. Guardrails: this block must not replace `TASK.md` or user intent, must not let the agent silently narrow scope, must not count as approval to skip mandatory gates or reviews, and reviewers must verify that the declared criteria still match the original task rather than a self-rewritten easier version. |
| T-079 | 🟦 TODO | P2 | workflow/assumptions-ambiguity-contract                                                                                           | Record explicit assumptions and ambiguity handling before implementation, forcing either `assumptions: none` or a structured list of behavior-affecting unknowns and requirement conflicts                               | gpt-5.4 | 2026-04-15 | balanced | The current workflow has planning and review stages, but it still allows an implementation agent to silently choose among ambiguous interpretations before the code exists. Add an early lifecycle artifact or task event that captures assumptions and unresolved ambiguities in a compact form. Guardrails: recorded assumptions must never act as implicit approval, must not authorize changing observable behavior without confirmation, must not bypass orchestrator lifecycle or mandatory reviews, and any ambiguity that changes behavior or contracts must still force an explicit stop or user clarification instead of a quiet unilateral choice. |
| T-139 | 🟦 TODO | P2 | workflow/active-task-single-run-enforcement                                                                                       | Enforce the single-active-task contract in runtime gates so a second task cannot enter or compile in the same worktree unless an explicit queued-multitask mode is active                                                | gpt-5.4 | 2026-04-19 | strict | A diagnostic `T-137` probe showed that a second task in the same dirty worktree could still pass `enter-task-mode`, `classify-change`, `load-rule-pack --stage POST_PREFLIGHT`, and `compile-gate` even though policy says only one task may be in active execution at a time. Add runtime enforcement or an explicit audited exception contract so the gate layer matches the documented workflow. Guardrails: do not break same-task resume, do not block intentionally read-only diagnostics unless they are explicitly modeled, and if future `T-109` queued multitask mode is active make that allowance explicit and auditable instead of silently allowing normal parallel task starts. |
| T-108 | 🟦 TODO | P2 | workflow/dirty-worktree-guided-launch                                                                                             | Replace the flat dirty-worktree start rejection with a guided launch decision that explains risks and offers explicit safe isolation modes                                                                               | gpt-5.4 | 2026-04-17 | balanced | The current workflow correctly protects scope integrity when the workspace was already dirty before task-mode entry, but the operator experience is too blunt: agents often report the run as impossible instead of guiding the user toward an auditable safe path. Add a guided launch contract for dirty worktrees that explains the concrete risks and offers explicit choices such as staged-scope start, explicit changed-file scope, isolation or sandbox mode, or cancel. Guardrails: do not silently continue an unsafe normal run, do not weaken dirty-baseline protection, keep the selected launch mode auditable in task-mode and preflight artifacts, and prefer machine-stable choices over free-form chat confirmation. |
| T-255 | 🟦 TODO | P2 | review/root-scratch-artifact-guard                                                                                                | Prevent reviewer and subagent scratch artifacts from being created in the repository root and route temporary review files only through `.review-temp` or canonical runtime review artifacts                             | gpt-5.4 | 2026-04-26 | balanced | A live `T-066` run left an untracked `temp-review.md` in the repository root even though review output should flow through `.review-temp/...` or canonical `runtime/reviews/...` artifacts. Add explicit reviewer-launch guidance plus a guard or doctor/completion diagnostic for forbidden root scratch files such as `temp-review.md`, `review-output.md`, and `review.md`. Guardrails: do not silently delete user files; do not touch canonical runtime artifacts; preserve path-based `record-review-result`; keep diagnostics clear before completion or commit; and add regressions proving root scratch artifacts are detected while valid `.review-temp` and `runtime/reviews` paths remain allowed. |
| T-140 | 🟦 TODO | P2 | reliability/planned-scope-foreign-diff-attribution                                                                                | Distinguish planned `--changed-file` scope from actual task-owned diff so foreign worktree changes cannot make a new task look `DIFF_PRESENT` or compile-ready                                                           | gpt-5.4 | 2026-04-19 | strict | A diagnostic `T-137` probe with explicit `--changed-file` scope and no task-specific edits reported `changed_files_count=11`, `additions_total=3629`, and `zero_diff_guard.status=DIFF_PRESENT` while the only real diff belonged to another task. Harden preflight and downstream gates so planned scope hints stay useful for early classification, but actual readiness, zero-diff evaluation, and compile/review progression depend on task-owned diff or audited no-op evidence. Guardrails: preserve clean-workspace planned-scope recovery semantics, do not regress dirty-worktree protection, and do not let foreign uncommitted changes satisfy another task's lifecycle evidence. |
| T-135 | 🟦 TODO | P2 | review/reviewer-identity-execution-mode-contract                                                                                  | Enforce coherent reviewer identity and execution-mode semantics so self-scoped, missing-identity, or fallback-shaped receipts cannot masquerade as independent delegated review                                          | gpt-5.4 | 2026-04-23 | strict | Review trust summaries and completion evidence must reject incoherent combinations such as `delegated_subagent` with self-scoped reviewer identity, `same_agent_fallback` with agent-scoped identity, or fallback receipts missing the explicit fallback reason. Guardrails: preserve high-signal degradation instead of silently coercing invalid receipts; keep same-agent receipts visibly unavailable for independent-review claims; and align trust-summary semantics with receipt validation and audit output. |
| T-198 | 🟦 TODO | P2 | review/path-based-pass-review-fidelity                                                                                            | Preserve raw reviewer evidence losslessly for path-based PASS review materialization and record fidelity explicitly in canonical artifacts and receipts                                                                  | gpt-5.4 | 2026-04-23 | strict | When a PASS review is normalized into canonical materialized output, the system must preserve raw reviewer evidence losslessly enough for later audit while recording the materialization fidelity and raw-output metadata in the receipt. Guardrails: keep path-based materialization deterministic; do not drop meaningful findings or residual-risk text just because the verdict is PASS; preserve cheap exact passthrough when normalization is unnecessary; and keep stdin-specific follow-up work isolated to `T-199`. |
| T-077 | 🟦 TODO | P3 | ux/reviewer-session-freshness-guard                                                                                               | Prevent resumed reviewer sessions from surfacing stale closed verdicts as if they belonged to the current review cycle                                                                                                   | gpt-5.4 | 2026-04-15 | balanced | In the live `T-040` run, resuming a previously used reviewer agent first surfaced an old completed verdict from an earlier cycle, and the orchestrator had to explicitly interrupt it with a fresh prompt before continuing. Add a freshness guard so resumed reviewer sessions are clearly marked stale for the current task cycle, or are forced through a fresh run path, instead of making the operator distinguish an old closed result from a new review outcome by hand. |
| T-081 | 🟦 TODO | P2 | review/surgical-scope-enforcement                                                                                                 | Strengthen review enforcement for surgical changes so reviewers explicitly flag drive-by refactors, unrelated cleanup, comment-only churn, and deletion of adjacent code outside the requested task scope                | gpt-5.4 | 2026-04-15 | strict | The workflow already controls task start scope well, but once the agent is inside a relevant file it can still widen the diff with opportunistic cleanup or neighboring refactors that were never requested. Extend review contracts and checklists to verify that changed files and edits remain tightly tied to preflight scope and task intent. Guardrails: this must not block necessary tightly coupled fixes when they are required for correctness and justified in review evidence, but it must prevent agents from using a legitimate entry point as a cover for unrelated edits, and it must not introduce any bypass around compile, review, doc-impact, or completion gates. |
| T-109 | 🟦 TODO | P2 | workflow/queued-multitask-sidecar-research                                                                                        | Add an explicit queued multitask mode with one active code-changing task and sidecar read-only research for later tasks, backed by a temporary session table artifact and final queue summary                            | gpt-5.4 | 2026-04-17 | balanced | A full multi-task parallel workflow in one worktree is too risky today, but there is still value in letting one agent manage a short queue of tasks within a session when only one task is allowed to change code at a time. Add an opt-in queued multitask mode where exactly one task may be active for implementation and gates, while later tasks are limited to read-only analysis, planning, evidence gathering, or reviewer-side research until they are promoted. Maintain a temporary session table artifact that tracks each queued task's summary, current state, findings, blockers, and commit suggestion, and allow the final user report to return that table for operator handoff. Guardrails: do not merge multiple tasks into one lifecycle, commit, or completion artifact; do not let sidecar tasks mutate code before they become active; preserve current single-active-task safety outside this explicit mode; and avoid requiring git worktree branching or automatic local commits in the first iteration. |
| T-347 | 🟦 TODO | P2 | workflow/parallel-provider-capability-detection                                                                                   | Detect whether the active provider can run true implementation sub-agents before parallel task execution                                                                                                                 | gpt-5.4 | 2026-05-02 | strict | Split from T-172. Add a machine-readable provider capability check for true implementation-lane parallelism, separate from reviewer delegation capability. Guardrails: if capability is unknown or unavailable, the coordinator must refuse or fall back to sequential execution; do not pretend parallelism exists in a single workspace. |
| T-348 | 🟦 TODO | P2 | workflow/parallel-task-independence-analysis                                                                                      | Analyze task independence, file overlap, shared contracts, and architectural coupling before approving parallel lanes                                                                                                    | gpt-5.4 | 2026-05-02 | strict | Split from T-172. Add a feasibility analysis that rejects or stacks tasks with explicit dependencies, overlapping files, shared schema or API risk, orchestrator-core coupling, config overlap, or broad refactor scope. Guardrails: do not classify tasks as parallel-safe solely because text diff scopes do not overlap; make refusal reasons auditable. |
| T-349 | 🟦 TODO | P2 | fixture-setup-deduplication | Consider deduplicating remediation-cycle integration test setup                                                                                                                                                          | gpt-5.4 | 2026-05-09 | fast | Deferred from T-319 refactor review artifact D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-319-refactor.md. Parent task: T-319. Review type: refactor. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-319-refactor.md. Original finding: Test setup duplication in tests/node/cli/commands/gates-review-cycle.test.ts is noticeable across the new cases. Non-blocking because the duplicated fixture setup is localized to integration-style coverage and improves scenario readability for the remediation boundary contract. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: test refactor only; preserve scenario readability and all remediation boundary assertions. Dumb-agent constraint: test files and test fixtures only; do not edit `src/**`, do not rename scenarios broadly, do not delete assertions, and do not change expected behavior. If deduplication becomes broad or confusing, stop after reporting candidate helper boundaries. |
| T-537 | 🟦 TODO | P5 | review/t536-code-review-full-suite-note                                                                                           | Reconcile T-536 code-review validation-boundary note after full-suite evidence exists                                                                                                                                    | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: code. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-code.md. Original finding: [follow-up] Full repository test suite was not run by this reviewer. Focused validation passed: Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: bookkeeping or review-output-normalization follow-up only; do not rerun T-536 lifecycle just to satisfy this generic validation-boundary note; if behavior is changed, keep strict deferred-finding enforcement for real actionable findings. |
| T-538 | 🟦 TODO | P5 | review/t536-code-review-targeted-full-suite-command-note                                                                          | Reconcile T-536 code-review targeted full-suite command note                                                                                                                                                             | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: code. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-code.md. Original finding: [follow-up] npm test -- tests/node/gates/full-suite-validation.test.ts Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: bookkeeping or review-output-normalization follow-up only; T-536 full-suite evidence already ran through `full-suite-validation`; do not weaken strict deferred-finding enforcement for real actionable findings. |
| T-539 | 🟦 TODO | P5 | review/t536-code-review-workflow-config-policy-command-note                                                                       | Reconcile T-536 code-review workflow-config policy command note                                                                                                                                                          | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: code. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-code.md. Original finding: [follow-up] npm test -- tests/node/cli/commands/gates-workflow-config-policy.test.ts Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: bookkeeping or review-output-normalization follow-up only; do not expand T-536 scope unless the command exposes a real regression. |
| T-540 | 🟦 TODO | P5 | review/t536-code-review-typecheck-note                                                                                            | Reconcile T-536 code-review typecheck validation-boundary note                                                                                                                                                           | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: code. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-code.md. Original finding: [follow-up] npm run typecheck Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: bookkeeping or review-output-normalization follow-up only; run typecheck only if this task is promoted, and do not weaken mandatory compile/full-suite gates. |
| T-541 | 🟦 TODO | P5 | review/t536-security-review-focused-validation-note                                                                               | Reconcile T-536 security-review focused-validation note                                                                                                                                                                  | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: security. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-security.md. Original finding: [follow-up] Focused review only. I ran `npm run build:node-foundation` and the targeted node tests for full-suite validation plus workflow-config policy; I did not run the entire repository suite. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: bookkeeping or review-output-normalization follow-up only; full-suite validation for T-536 already passed through the gate. |
| T-542 | 🟦 TODO | P5 | tests/t536-compile-completion-fallback-negative-coverage                                                                          | Consider narrower compile/completion fallback rejection coverage from T-536 test review                                                                                                                                  | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: test. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-test.md. Original finding: None blocking. A future cleanup could add a narrower assertion that compile/completion explicitly reject protected-manifest fallback when task-mode baseline is absent, but current downstream tests already exercise the practical failure modes. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: test hardening only; do not weaken workflow-config protection, protected-manifest fallback rules, compile/full-suite/completion gates, or review trust. |
| T-543 | 🟦 TODO | P5 | tests/t536-compile-completion-fallback-negative-coverage-duplicate                                                                | De-duplicate or resolve repeated T-536 test-review fallback coverage follow-up                                                                                                                                           | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: test. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-test.md. Original finding: None blocking. A future cleanup could add a narrower assertion that compile/completion explicitly reject protected-manifest fallback when task-mode baseline is absent, but current downstream tests already exercise the practical failure modes. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: bookkeeping/test-hardening only; this row exists because strict completion materialized the duplicate deferred text separately. Prefer resolving the duplicate-source normalization issue before adding duplicate tests. |
| T-544 | 🟦 TODO | P5 | tests/t536-helper-generated-manifest-risk                                                                                         | Review helper-generated protected manifest reliance introduced during T-536                                                                                                                                              | gpt-5.4 | 2026-05-13 | fast | Parent task: T-536. Review type: test. Source artifact: D:/Projects/garda-agent-orchestrator/garda-agent-orchestrator/runtime/reviews/T-536-test.md. Original finding: [follow-up] Fixture broadening in [gate-test-seed-helpers.ts](</mnt/d/Projects/garda-agent-orchestrator/tests/node/cli/commands/gate-test-seed-helpers.ts:217>) is pragmatic and keeps legacy fixtures passing, but it means some older tests now rely on helper-generated protected manifests rather than hand-authored setup. Full-suite validation passed: `NODE_FOUNDATION_TEST_OK`, 5429 tests, 5423 passed, 0 failed, 6 skipped. Justification: Preserved from raw reviewer output during PASS review normalization. Guardrails: test fixture hardening only; do not weaken task-mode workflow-config baseline enforcement or protected-manifest drift checks. |
<!-- garda-agent-orchestrator:managed-end -->

## Блок очереди

Ручная группировка синхронизирована с верхней `Active Queue` на 2026-05-18 после переноса всех `DONE` строк в `TASK_DONE.md`. В этом блоке перечислены только non-DONE задачи. Базовый UI уже практически сделан; оставшиеся UI-adjacent пункты ниже считаются polish/metadata, а не отдельным крупным UI-релизом.

### Release 1.1.x — до-рейлз ценность для пользователей и усиление доверия

Задачи, которые сильнее всего влияют на качество релиза: ускорение обязательной проверки, защита секретов, доверие к делегированию, защита control-plane и понятный closeout.

- `T-309` — Подготовка `full-suite` через prebuilt-артефакты и шарды. Что нужно: пересобрать скрипты один раз и запускать все node-тесты порциями. Для чего: сократить wall-clock время обязательной проверки без потери покрытия.
- `T-240` — Маскирование секретов во всех артефактах. Что нужно: скрыть секреты в событиях, материалах ревью, выводе команд, логах компиляции и доказательствах full-suite. Для чего: исключить утечку чувствительных данных в отчётах и логах.
- `T-241` — Политика происхождения, целостности и подписи self-update. Что нужно: добавить проверяемую цепочку источника/хэша/подписи для автообновлений. Для чего: сделать процесс обновления воспроизводимым и защищённым от подмены.
- `T-566` — Исправить stale `UpdatedVersion` после успешного update apply и цветом выделить applied-update/new-version сообщения. Что нужно: читать итоговую версию после deferred sync и улучшить human-output coloring без изменения JSON/no-color контракта. Для чего: пользователь после обновления должен сразу видеть корректную версию и заметное сообщение о новой/применённой версии.
- `T-567` — Добавить server-owned idle shutdown для `garda ui`. Что нужно: сервер считает неактивность, браузер показывает warning slider/countdown, активность сбрасывает таймер, после истечения сервер полностью завершается. Для чего: UI не висит бесконечно и не притворяется, что может сам себя заново запустить после shutdown.
- `T-568` — Добавить i18n для `garda ui` с языками `en` и `ru`, но расширяемой архитектурой под будущие языковые пакеты. Что нужно: вынести UI-строки в словари, добавить панель выбора языка и fallback на English, не переводить команды/config keys/raw artifacts. Для чего: локальный dashboard сразу работает на двух языках, а `de`/`uk` можно добавить позже без переписывания UI.
- `T-336` — Надёжный режим `no-delegate` и откат при небезопасном окружении. Что нужно: добавить безопасный fallback, когда делегирование невозможно или рискованно. Для чего: не блокировать работу только из-за невозможности делегирования, сохраняя безопасность.
- `T-337` — Валидация цели делегирования по repo-root, package-id и containment пути. Что нужно: явно проверять, куда делегируется запуск. Для чего: не допустить делегирование вне ожидаемого репозитория/пакета.
- `T-569` — Default output path для `build-review-context`. Что нужно: если `--output-path` не задан, gate сам пишет canonical context artifact и возвращает путь/hash агенту. Для чего: убрать ловушку, где команда успешно завершается, но агент не получает созданный контекст.
- `T-570` — Gate-owned capture сырого ответа ревьювера. Что нужно: разрешить main agent передать точный текст sub-agent ответа в gate, а gate сам сохранит raw artifact и проверит формат. Для чего: не требовать от reviewer sub-agent умения писать файлы и не поощрять ручную реконструкцию вывода.
- `T-571` — Gate-owned trust/timing UX для reviewer flow. Что нужно: все timestamps писать системным временем внутри gates/runtime, скрыть trust-chain механику от агента и выдавать только понятные действия/ошибки. Для чего: агент запускает реальное ревью и записывает фактический output, а подделки/старые артефакты ловит gate.
- `T-572` — Не запускать mandatory `full-suite-validation` на baseline-only pre-implementation цикле. Что нужно: `after_compile_before_reviews` должен требовать suite после реальных изменений и актуального compile, но не после пустого baseline preflight. Для чего: сохранить full-suite перед ревью, но убрать бессмысленный долгий запуск до реализации.
- `T-573` — Запретить поздний rerouting ревью после уже записанного результата. Что нужно: `record-review-routing` должен блокировать новую routing telemetry для того же review type после `REVIEW_RECORDED`, если не начат явный свежий review/restart цикл. Для чего: не получать ситуацию, где review gate проходит, а completion потом падает из-за другого reviewer id.
- `T-574` — Выровнять проверки provenance между `required-reviews-check` и `completion-gate`. Что нужно: review gate должен ловить те же конфликты identity/mode/context/routing, что и completion. Для чего: агент узнает о проблеме сразу до `REVIEW_GATE_PASSED`, а не в финальном closeout.
- `T-575` — Не превращать не-actionable PASS notes в обязательные F-задачи. Что нужно: validation-boundary notes сохранять в raw output/audit, но не направлять агента в `Deferred Findings`, если это не реальная отложенная работа. Для чего: убрать мусорные F tasks и ложные strict blockers без ослабления настоящих findings.
- `T-576` — Сделать human output workflow settings команд нормальным и цветным. Что нужно: убрать misleading `ChangedFields` при `NO_CHANGE`, разделить requested/actual changed fields, сгруппировать настройки и сохранить JSON/audit контракты. Для чего: оператор понимает, что реально изменилось, а automation продолжает читать `--json`/audit.
- `T-577` — Не блокировать следующую ordinary task после audited `workflow set` из-за stale protected manifest. Что нужно: после легальной workflow-config мутации обновлять/согласовывать trusted manifest или давать точную safe repair-команду. Для чего: `workflow set` не должен оставлять Chronobot в состоянии, где `enter-task-mode` считает чистый workspace грязным.
- `T-578` — Сделать `doctor` failure summary читаемым и actionable. Что нужно: выводить сверху основные blockers, выделять fail/drift цветом и давать конкретный next command, сохранив `doctor --json`. Для чего: при `Doctor: FAIL` оператор сразу видит причину, а не ищет её среди PASS-разделов.
- `T-579` — Сверить и синхронизировать Node baseline. Что нужно: решить, поддерживаем ли Node 20/22 или остаёмся на `>=24.0.0`, и привести package engines, constants, docs, doctor, CI/release validation и тесты к одному контракту. Для чего: doctor не должен противоречить ожидаемому runtime support.
- `T-580` — Добавить managed `.agentignore` block для active Garda mode. Что нужно: скрывать тяжёлые generated/runtime артефакты, но оставлять instructions/config/bin видимыми; `garda off` должен добавлять полный `garda-agent-orchestrator/` ignore поверх active block, а `garda on` снимать только off-mode часть. Для чего: меньше мусора в агентском контексте без поломки CLI/gates.
- `T-581` — Стабилизировать flaky temp git fixtures в full-suite. Что нужно: разобраться с повторяющимся Windows temp `git init` / `.git/config Permission denied` в тестах вроде `gates-protected-drift`, усилить изоляцию/retry/диагностику fixture setup. Для чего: не тратить циклы на случайные падения full-suite и при этом не маскировать настоящие failures.
- `T-582` — Принимать нормальные варианты `none` в PASS review. Что нужно: разрешить `none` в любом регистре и очевидные no-risk формулировки вроде `None significant...`, но по-прежнему блокировать настоящие findings/deferred/residual risks. Для чего: не гонять лишний reviewer restatement из-за косметики.
- `T-583` — Улучшить UX `project-memory-impact --confirm-updated`. Что нужно: печатать готовую команду с нужными `--updated-memory-file` или безопасно выводить список из affected files. Для чего: агент не должен угадывать обязательные audit flags после того, как уже обновил memory-файлы.
- `T-584` — Не инвалидировать fresh review context поздним startup/preflight refresh. Что нужно: разобраться, почему после подготовки review context поздний TASK_ENTRY/handshake/shell-smoke/classify-change меняет preflight hash и заставляет повторять review lane. Для чего: убрать лишние повторные reviewer циклы, не ослабляя binding review context к актуальному scope.
- `T-585` — Не подставлять Node/npm defaults при `init` для Java/unknown-stack проектов. Что нужно: при setup/init не заменять реальные проектные команды шаблонным `npm test`, а требовать discovery/явное подтверждение или ставить unconfigured sentinel; сохранить `npm test` там, где это действительно Node/orchestrator repo. Для чего: агентский `init` не должен ломать Gradle-проекты и делать full-suite команду невалидной.
- `T-239` — Жёсткое сканирование и доверенные границы protected control-plane. Что нужно: усилить контроль симлинков и границ кэша. Для чего: убрать уязвимые зоны для подмены и несанкционированного доступа.
- `T-219` — Политика соответствия `target-root` и `source-bundle` для каждой команды. Что нужно: задать явное правило совпадения источника и корня команды. Для чего: уменьшить ошибки запуска и ошибки делегирования.
- `T-261` — Канонические алиасы провайдеров (`Copilot` и вариации имён). Что нужно: свести все варианты к единой canonical-подписи. Для чего: стабилизировать маршрутизацию и отчётность по провайдерам.
- `T-232` — Аудитируемая обёртка промежуточных команд. Что нужно: сделать прозрачный запуск целевых `npm test`/`typecheck` и сохранение полного вывода в артефакт. Для чего: получать и экономить вывод корректно, без потери трассировки.
- `T-234` — Единый контракт отчёта о компрессии вывода. Что нужно: синхронизировать метрики и формулировки между gate-ами, `stats`, аудитом и финальным closeout. Для чего: убрать расхождения в понимании “кто что и как экономит”.
- `T-269` — Практичные подсказки `doc-impact-gate` для граничных enum-значений. Что нужно: дать операторам конкретные действия при near-miss значениях. Для чего: быстрее закрывать false-positive и не терять покрытие.
- `T-267` — Closeout показывает фактическую глубину префлайта и причину эскалации. Что нужно: явно выводить эффективный `depth` и причину повышения режима. Для чего: чтобы оператор видел реальный уровень проверки и её основание.

### Release 1.1.x polish — доработка подтверждений и небольшой UX/test hardening

Небольшие follow-up задачи вокруг уже внедрённого контракта `operator-confirmation` / `workflow set`. Их можно закрывать партиями, но они не должны тормозить security/trust блок выше.

- `T-547-2-F6` — Вынести общий текст подсказок подтверждения для `workflow set`. Что нужно: убрать дублирование одинаковых формулировок. Для чего: не допускать разъезда требований между командами и help-текстами.
- `T-547-2-F7` — Переиспользовать единые тестовые helper-ы подтверждений `workflow set`. Что нужно: убрать частичный дублирующий код в тестах. Для чего: проще поддерживать поведение подтверждений и быстрее обновлять тесты.
- `T-547-2-F9` — Проверить дрейф текста подтверждений между help и onboarding. Что нужно: сопоставить формулировки в разных поверхностях. Для чего: исключить ситуацию, когда оператор видит разные инструкции.
- `T-547-2-F10` — Сверить downstream-автоскрипты и доки с новым контрактом `workflow set`. Что нужно: обновить интеграции, где требуется обязательный `--operator-confirmed`. Для чего: снизить сбои внешней автоматизации после ужесточения контракта.
- `T-547-1-F1` — Подсказки по значениям timestamp в командах восстановления task-mode. Что нужно: явно описать требования к `--operator-confirmed-at-utc`. Для чего: меньше ошибок при ручном вводе и проще поддерживать консистентную операционку.
- `T-547-1-F6` — Регрессия на будущее время для защищённого подтверждения task-mode. Что нужно: покрыть тестом случай `timestamp` в будущем. Для чего: закрыть риск обхода проверки свежести.
- `T-547-1-1-F1` — Уточнить условные флаги подтверждения в помощи `restart-coherent-cycle`. Что нужно: корректно описать, когда флаги обязательны. Для чего: уменьшить путаницу операторов в help-окне.
- `T-547-1-1-F3` — Проверить строгость разбора литерала `yes`. Что нужно: решить, принимать ли только буквальный `yes`. Для чего: убрать неоднозначность и усилить policy-поведение.
- `T-547-1-1-F4` — Отрицательный путь подтверждения для `restart-coherent-cycle`. Что нужно: добавить тест отказа при невалидном подтверждении. Для чего: зафиксировать ожидаемое поведение защиты.
- `T-547-1-1-F6` — Проверка отклонения future-skew timestamp в task-mode. Что нужно: протестировать случай с сильно смещённым во времени значением. Для чего: не допустить принятие просроченных/некорректных подтверждений.
- `T-547-1-1-F7` — Проверка формата неправильного `--operator-confirmed-at-utc`. Что нужно: добавить тест с некорректным ISO-форматом. Для чего: стабилизировать валидацию парсера на уровне task-mode.
- `T-498` — Необязательная метка `created` в `Notes` без новых колонок. Что нужно: сохранять дату создания задачи рядом с notes. Для чего: улучшить сортировку и отчёты без ломки таблицы.
- `T-106` — Дополнительный раздел final-closeout для неблокирующих вне-скоуп находок. Что нужно: явно перечислять такие находки. Для чего: не терять полезную информацию и не смешивать с blockers.
- `T-463` — Единое доку-паттерн для формата TASK.md по строгим deferred follow-ups. Что нужно: зафиксировать правила оформления только в docs. Для чего: единообразие deferred задач и меньше ручной нормализации.

### Release 1.2.0 — наблюдаемость, контракты событий и диагностика оператора

После UI baseline нужны стабильные contracts для дешевых отчетов, диагностики и внешних consumers.

- `T-217` — Версионируемая публичная схема `runtime/task-events/*.jsonl`. Что нужно: определить структуру событий и схему совместимой версии. Для чего: чтобы внешние/внутренние потребители стабильно читали последние циклы.
- `T-141` — Приведение `status`, `why-blocked`, `task-audit-summary` в единый вид для частичных и диагностических циклов. Что нужно: убрать расхождения в этих полях. Для чего: меньше “ложных” блоков в отчётах и воспроизводимый recovery.
- `T-169` — Неблокирующие уведомления о наличии обновлений при старте сессии/финальном отчёте. Что нужно: дать hints без остановки потока. Для чего: оператор не пропускал бы важные сигналы, но и не терял времени.
- `T-225` — Долговечный реестр последующих проблем. Что нужно: фиксировать follow-up надёжно. Для чего: не терять найденные, но не срочные issues.
- `T-338` — Отражение состояния доверия делегирования в диагностике и операционных доках. Что нужно: показать статус trust/delegation прямо в нужных местах. Для чего: быстрее разруливать инциденты и вопросы доверия.
- `T-218` — Базовый CI security pipeline. Что нужно: добавить обязательный проход secret scan, risk зависимостей, SBOM и отчёт источников обновлений. Для чего: повысить базовую защищённость релизного цикла.
- `T-221` — Политика “полный пакет vs slim package”. Что нужно: выбрать и закрепить, какие артефакты включаются в дистрибутив. Для чего: убрать неопределенность в поставках и восстановлении.
- `T-233` — Типизированный manifest настроек workflow. Что нужно: описать дефолты, правила merge `preserve/seed`, CLI-поверхности и валидацию. Для чего: управлять настройками без ручного, разобщённого парсинга.

### Release 1.3.0 — провайдер-нативные поверхности

Интеграция по провайдер-нативным возможностям должна идти единым блоком.

- `T-162` — Канонический формат provider-native skills/instructions. Что нужно: задать единый контракт описания для разных поставщиков. Для чего: унифицировать, как навигировать и хранить инструкции.
- `T-166` — Модель поставщика для общих entrypoint-файлов. Что нужно: формализовать, как один root entrypoint выбирается под разных провайдеров. Для чего: убрать расхождения в старте сессий и routing.
- `T-163` — Перенос навыков в реальный provider-native формат файлов. Что нужно: материализовать `skills` в нативной структуре. Для чего: сделать их совместимыми с экосистемой провайдеров.
- `T-164` — Provider-native адаптеры для rules/guidelines/commands/shared entrypoints. Что нужно: добавить прослойку интеграции под каждый runtime-провайдер. Для чего: единообразно использовать одни и те же правила через разные среды.
- `T-165` — Проверка установки/обновления/doctor для provider-native поверхностей. Что нужно: добавить валидацию жизненного цикла этих файлов. Для чего: ловить поломки раннее, до runtime usage.

### Release 1.4.0 — контракты workflow и дисциплина планирования

Контракты lifecycle, которые повышают предсказуемость для строгих и неоднозначных работ.

- `T-339` — Канонический типизированный манифест фаз lifecycle. Что нужно: формализовать схему этапов задачи. Для чего: runtime и отчёты не гадали, на каком шаге мы находимся.
- `T-340` — Проверка docs/skills/workflow по lifecycle-манифесту. Что нужно: валидировать соответствие описаний фактическому манифесту. Для чего: не допустить рассинхрона между документами и кодом.
- `T-341` — Использование манифеста в runtime и `next-step`, где важен порядок. Что нужно: привязать выполнение к manifest-фазам. Для чего: повысить предсказуемость переходов.
- `T-342` — Генерация/обновление человекочитаемых lifecycle-доков из манифеста. Что нужно: дергать docs из единого источника. Для чего: убрать ручную правку и рассинхрон текста.
- `T-107` — Порог старта на основе плана для крупных/рискованных strict задач. Что нужно: требовать дополнительный plan-check прежде чем заходить в реализацию. Для чего: снижать дорогие ошибки на ранней стадии.
- `T-078` — Структурированные критерии успеха до начала implementation. Что нужно: зафиксировать что считать “готово”. Для чего: не стартовать со слабой валидацией.
- `T-079` — Явный артефакт с допущениями и разбором неоднозначностей. Что нужно: записывать неочевидные решения до них. Для чего: снизить возвраты и конфликтные толкования.
- `T-139` — Жёсткое правило одного активного кода-задачи в runtime gate. Что нужно: запретить параллельное кодовое выполнение задач в одном контуре. Для чего: контрольный механизм против drive-by влияний.
- `T-108` — Направленное решение для dirty-worktree вместо жёсткого отказа. Что нужно: оценивать риски и выбирать безопасный запуск. Для чего: не блокировать даже при аккуратных локальных изменениях.
- `T-358` — Префлайт strict-задач через декомпозицию по prompt-first. Что нужно: требовать разбор задачи и подзадач до стартового исполнения. Для чего: упростить масштабирование сложных задач без потери контроля.

### Release 1.5.0 — доверие рецензии и контроль scope

Задачи trust/ревью с высокой ценностью и повышенным риском, поэтому оставлены в отдельной очереди после основного hardening.

- `T-255` — Запрет на временные reviewer/subagent артефакты в корне репозитория. Что нужно: убрать мусорные файлы из рабочей корневой области. Для чего: не засорять артефакты и не влиять на запуск.
- `T-140` — Чёткое разделение запрошенного scope и фактического diff/чужих изменений. Что нужно: фиксировать и проверять, что изменяет именно задача. Для чего: не маскировать нерелевантные правки.
- `T-135` — Единая трактовка identity рецензента и режима исполнения. Что нужно: стабилизировать роль/правила reviewer-сессий. Для чего: не получать конфликтные проверки из-за нестрогой семантики.
- `T-198` — Путь-ориентированная точность PASS-ревью и сохранение сырых доказательств без потери данных. Что нужно: сохранить весь raw output и привязку к путям. Для чего: повысить доверие к PASS и проверяемость.
- `T-077` — Защита свежести reviewer-сессии после resume. Что нужно: не продолжать устаревшую сессию как актуальную. Для чего: не пропускать изменения контекста проверки.
- `T-081` — Точечная защита scope от побочных правок. Что нужно: автоматически ловить drive-by edits. Для чего: не размывать ответственность и уменьшить шум.
- `T-335-F1` — Покрытие on-disk spoofing для классификации доверия делегирования в runtime. Что нужно: добавить тесты на подмену файловых следов. Для чего: точнее отличать поддельное/настоящие runtime окружения.
- `T-335-F2` — Более сильное доказательство provenance runtime, чем эвристики по структуре пакета. Что нужно: усилить трассировку происхождения. Для чего: снизить риск ложных доверенных решений.
- `T-335-F3` — Рефактор решения доверия делегированию. Что нужно: упростить и сделать предсказуемым путь принятия trust-решения. Для чего: уменьшить баги из-за сложной ветвистости.
- `T-335-F4` — Тесты branch-lock на уровне wrapper для deployed/unrecognized пакетов. Что нужно: закрыть сценарии с неизвестными runtime пакетами. Для чего: не дать несанкционированное поведение на прод-среде.

### Release 1.6.0 — parallel and multitask execution

Отдельный capability-релиз, потому что это меняет модель работы с workspace/lane isolation.

- `T-109` — Очередь multitask: боковые исследования и одна активная код-задача. Что нужно: ввести временный режим, где исследования ведутся параллельно, но код пишет только основной таск. Для чего: ускорить анализ без потери безопасности контекста.
- `T-347` — Определение, умеет ли провайдер запускать настоящих implementation-сабагентов. Что нужно: детектировать реальную параллельную способность среды. Для чего: не обещать параллелизм без технической базы.
- `T-348` — Проверка независимости задач перед параллельным запуском. Что нужно: анализ пересечений файлов, контрактов и зависимостей. Для чего: запускать параллельно только безопасные задачи.

### Release 1.7.0 — test runtime and refactor cleanup

Низкорисковые или поддерживающие задачи после основных пользовательских hardening work.

- `T-096` — Сократить зависимость `gates.test.ts` от `concurrency: false`, где это возможно. Что нужно: убрать скрытое состояние между тестами или явно его изолировать. Для чего: сделать тесты стабильнее и быстрее.
- `T-297` — Разбить `task-events-summary.ts` на модули парсинга/агрегации/рендера. Что нужно: выделить ответственности. Для чего: упростить поддержку без изменения поведения.
- `T-349` — Убрать дублирование setup в интеграционных remediation-тестах, если это не ухудшает читаемость. Что нужно: нормализовать повторяющиеся подготовительные блоки. Для чего: меньше дублей и меньше шума при правках.
- `T-537` — Закрыть расхождение code-review T-536 по границе full-suite validation. Что нужно: отразить, что проверка уже есть/нужные команды задокументированы. Для чего: убрать незакрытую follow-up нагрузку.
- `T-538` — Закрыть расхождение по целевой full-suite команде в T-536 code-review. Что нужно: синхронизировать формулировки и артефакты. Для чего: корректный аудит и отсутствие ложных замечаний.
- `T-539` — Закрыть расхождение по `workflow-config policy` в T-536 code-review. Что нужно: привести note в соответствие факту проверки. Для чего: уменьшить ручную поддержку чеклистов.
- `T-540` — Закрыть расхождение по typecheck-границе в T-536 code-review. Что нужно: подтвердить границу обязательной проверки. Для чего: не запускать лишних задач повторно без причины.
- `T-541` — Закрыть расхождение по focused-validation в T-536 security-review. Что нужно: зафиксировать, какие проверки уже выполнены. Для чего: упростить ревизию security evidence.
- `T-542` — Добавить более узкое покрытие для отказа compile/completion fallback. Что нужно: проверить сценарий явного reject. Для чего: зафиксировать контракт поведения.
- `T-543` — Убрать повторяющийся follow-up по fallback-coverage T-536. Что нужно: дедуплицировать требования. Для чего: сократить технический долг в TASK-потоке.
- `T-544` — Проверить зависимости от helper-сгенерированных protected manifest. Что нужно: понять, где тесты слишком опираются на автогенерацию. Для чего: сохранить стабильность и предсказуемость regression suite.
