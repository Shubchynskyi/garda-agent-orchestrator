/**
 * Aggregator stub — imports the decomposed install test suites so the
 * original test path continues to work.  Each focused suite lives in
 * its own file; see install-core.test.ts, install-provider-bridges.test.ts,
 * and install-preserve-update.test.ts.
 */
import './install-core.test';
import './install-provider-bridges.test';
import './install-preserve-update.test';
