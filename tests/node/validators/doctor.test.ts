/**
 * Aggregator stub — imports the decomposed doctor test suites so the
 * original test path continues to work.  Each focused suite lives in
 * its own file; see doctor-run-doctor.test.ts, doctor-evidence-collectors.test.ts,
 * and doctor-formatting.test.ts.
 */
import './doctor-run-doctor.test';
import './doctor-evidence-collectors.test';
import './doctor-formatting.test';
