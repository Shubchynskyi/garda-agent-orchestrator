/**
 * Compatibility facade for the dependency-safe core path helpers.
 *
 * New low-level consumers should import from `src/core/orchestrator-paths`.
 */
export * from '../../core/orchestrator-paths';
